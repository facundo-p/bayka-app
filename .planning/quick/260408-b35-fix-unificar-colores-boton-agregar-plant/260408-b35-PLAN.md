---
phase: quick
plan: 260408-b35
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/screens/AdminScreen.tsx
autonomous: true
requirements: [VISUAL-UNIFY]
must_haves:
  truths:
    - "The '+' button in AdminScreen header has the same color composition as the download button in PlantacionesScreen header"
    - "Both header action buttons use solid primary background with white icon"
  artifacts:
    - path: "mobile/src/screens/AdminScreen.tsx"
      provides: "Unified header button styling"
      contains: "colors.primary"
  key_links: []
---

<objective>
Unify the "+" button in AdminScreen header to match the color composition of the PlantacionesScreen header button (download icon).

Purpose: Visual consistency across screens — both header action buttons should look the same.
Output: AdminScreen header button with solid primary background and white icon (matching PlantacionesScreen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@mobile/src/screens/AdminScreen.tsx
@mobile/src/screens/PlantacionesScreen.tsx
@mobile/src/theme.ts

<interfaces>
<!-- Current AdminScreen "+" button (lines 498-504): -->
<!-- backgroundColor: colors.primaryBg (#EDF4F9 — light blue bg) -->
<!-- Icon color: colors.plantation (#99B95B — olive green) -->
<!-- Size: width 40, height 40 -->

<!-- Target: PlantacionesScreen download button (lines 236-242): -->
<!-- backgroundColor: colors.primary (#0A3760 — solid dark blue bg) -->
<!-- Icon color: colors.white (#FFFFFF) -->
<!-- Size: width 34, height 34 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify AdminScreen header "+" button colors to match PlantacionesScreen</name>
  <files>mobile/src/screens/AdminScreen.tsx</files>
  <action>
In AdminScreen.tsx, update two things:

1. In the `rightElement` JSX (around line 498-504), change the Ionicons color from `colors.plantation` to `colors.white`:
   ```
   <Ionicons name="add" size={24} color={colors.white} />
   ```

2. In the `headerAddBtn` style (around line 693-700), change `backgroundColor` from `colors.primaryBg` to `colors.primary`, and adjust size to match PlantacionesScreen's `catalogButton` (width/height 34):
   ```
   headerAddBtn: {
     width: 34,
     height: 34,
     borderRadius: borderRadius.full,
     backgroundColor: colors.primary,
     alignItems: 'center',
     justifyContent: 'center',
   },
   ```

Also reduce the icon size from 24 to 18 to fit the smaller button, matching PlantacionesScreen's icon size:
   ```
   <Ionicons name="add" size={18} color={colors.white} />
   ```

This produces the exact same visual composition: solid `colors.primary` circle with white icon, same dimensions (34x34).
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Trabajos/BaykaApp/bayka-app-redesign && grep -n "headerAddBtn" mobile/src/screens/AdminScreen.tsx && grep -n "colors.white" mobile/src/screens/AdminScreen.tsx | grep -i "add\|icon.*add" || grep -A2 "name=\"add\"" mobile/src/screens/AdminScreen.tsx</automated>
  </verify>
  <done>AdminScreen header "+" button uses backgroundColor: colors.primary with white icon (size 18) in a 34x34 circle — identical composition to PlantacionesScreen's catalogButton.</done>
</task>

</tasks>

<verification>
- Visual: Both AdminScreen "+" and PlantacionesScreen download button render as solid dark blue circles with white icons
- Code: headerAddBtn style uses colors.primary (not colors.primaryBg), icon uses colors.white (not colors.plantation)
</verification>

<success_criteria>
The "+" button on AdminScreen header is visually identical in color composition to the download button on PlantacionesScreen header: solid primary-colored circle with white icon, same size (34x34).
</success_criteria>

<output>
After completion, create `.planning/quick/260408-b35-fix-unificar-colores-boton-agregar-plant/260408-b35-SUMMARY.md`
</output>
