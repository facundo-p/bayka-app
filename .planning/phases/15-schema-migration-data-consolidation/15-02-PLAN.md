---
phase: 15
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/012_parcelas_and_rename.sql
  - scripts/verify-012.sh
autonomous: false
requirements:
  - PARC-02
  - PARC-03
  - PARC-05
  - PARC-06
  - PARC-07
  - PARC-09
  - MIGR-02
tags: [supabase, postgres, ddl, rls, rpc, migration, v1.1]

user_setup:
  - service: supabase
    why: "Aplicar migration 012 vía dashboard SQL editor (per CONTEXT.md Claude's Discretion + RESEARCH Open Q2)"
    env_vars: []
    dashboard_config:
      - task: "Ejecutar contenido de supabase/migrations/012_parcelas_and_rename.sql en SQL editor del proyecto Supabase"
        location: "Supabase dashboard → SQL Editor → New query → paste + Run"
      - task: "Verificar que toda la transacción committeó (no hay BEGIN abierto)"
        location: "Misma SQL Editor session — al final ejecutar SELECT 1; debe responder sin error"

must_haves:
  truths:
    - "Tabla `parcelas` existe en Supabase con FK a `plantations`, CHECK descripcion ≤ 10000, RLS habilitada"
    - "Tabla `subgroups` renombrada a `groups`; `groups.parcela_id` (uuid, NULL por ahora) referencia `parcelas.id`"
    - "Columna `trees.subgroup_id` renombrada a `trees.group_id`; FK constraint actualizada"
    - "Índices únicos de Grupo cambiaron a `(parcela_id, codigo)` y `(parcela_id, nombre)` (DROP de los viejos per-plantation)"
    - "CHECK `tipo IN ('linea','bosquete')` activo en `groups`"
    - "RLS policies de `trees` (010 + 011) recreadas con referencia a `groups` y `group_id`"
    - "RPC `sync_subgroup` recreada con body actualizado (FROM groups, INSERT INTO groups, group_id)"
    - "RLS policies de `parcelas` permiten SELECT a authenticated y INSERT/UPDATE solo a plantation_users"
  artifacts:
    - path: "supabase/migrations/012_parcelas_and_rename.sql"
      provides: "DDL Supabase: rename + parcelas + RLS + RPC update — todo en BEGIN..COMMIT"
      contains: "CREATE TABLE parcelas"
    - path: "scripts/verify-012.sh"
      provides: "Script bash que ejecuta queries SQL contra Supabase para verificar post-DDL"
      contains: "groups"
  key_links:
    - from: "supabase/migrations/012_parcelas_and_rename.sql"
      to: "RLS policies en trees"
      via: "DROP POLICY + CREATE POLICY con group_id"
      pattern: "Plantation members can update trees"
    - from: "supabase/migrations/012_parcelas_and_rename.sql"
      to: "RPC sync_subgroup"
      via: "CREATE OR REPLACE FUNCTION con FROM groups"
      pattern: "FROM groups"
    - from: "supabase/migrations/012_parcelas_and_rename.sql"
      to: "parcelas table"
      via: "CHECK char_length(descripcion) <= 10000"
      pattern: "char_length\\(descripcion\\)"
---

<objective>
Aplicar todos los cambios de schema en Supabase production via migration `012_parcelas_and_rename.sql`: crear tabla `parcelas` (con CHECK descripcion + RLS), renombrar `subgroups` → `groups`, agregar `groups.parcela_id` FK, swap unique indexes a per-parcela, actualizar tipo CHECK (`linea | bosquete`), renombrar `trees.subgroup_id` → `trees.group_id`, **recrear policies 010 y 011 con `group_id`** (Pitfall 2), y **recrear RPC `sync_subgroup` con body que referencia `groups`/`group_id`** (Pitfall 3).

Purpose: Sin estos cambios atómicos, post-rename el sync se rompe inmediatamente: las policies RLS y la RPC referencian la tabla y columna viejas por nombre. La migration 012 es la única forma de mantener el sync funcional durante el window coordinado (D-01).

Output: Archivo SQL versionado + script bash de verificación + migration aplicada en Supabase production. **NO incluye** el CHECK de `estado` que elimina `'sincronizada'` — ese cambio va en 013 después del UPDATE de normalización (Pitfall 7).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-schema-migration-data-consolidation/15-CONTEXT.md
@.planning/phases/15-schema-migration-data-consolidation/15-RESEARCH.md
@.planning/phases/15-schema-migration-data-consolidation/15-PATTERNS.md
@.claude/CLAUDE.md
@supabase/migrations/001_initial_schema.sql
@supabase/migrations/006_add_cascade_deletes.sql
@supabase/migrations/009_sync_subgroup_update_trees.sql
@supabase/migrations/010_trees_update_policy.sql
@supabase/migrations/011_trees_insert_policy_members.sql

<interfaces>
<!-- RPC actual sync_subgroup (a re-crear con tabla nueva) -->
La RPC actual referencia `subgroups` directamente en líneas 19, 29 — debe re-crearse con `FROM groups` y `INSERT INTO groups`.

<!-- Policies actuales 010/011 (a DROP + CREATE) -->
- 010: `WHERE sg.id = trees.subgroup_id` → debe ser `trees.group_id`
- 011: `WHERE sg.id = subgroup_id` → debe ser `group_id`
- JOIN `FROM subgroups sg` → `FROM groups sg` (alias preservado para mínima diferencia)

<!-- Names de constraints UNIQUE de subgroups en Supabase (Pitfall 4) -->
Per 001_initial_schema.sql líneas 58-68, las constraints son UNIQUE inline (no índices nombrados explícitamente):
- `unique (plantation_id, codigo)` → nombre auto-generado típicamente `subgroups_plantation_id_codigo_key`
- `unique (plantation_id, nombre)` → nombre auto-generado típicamente `subgroups_plantation_id_nombre_key`
DROP debe usar IF EXISTS y los nombres exactos. Si el rename de tabla los renombra automáticamente a `groups_*_key`, ajustar.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Escribir SQL 012_parcelas_and_rename.sql (DDL completo en BEGIN..COMMIT)</name>
  <files>supabase/migrations/012_parcelas_and_rename.sql</files>
  <read_first>
    - supabase/migrations/001_initial_schema.sql (patrón CREATE TABLE + CHECK + UNIQUE + RLS base)
    - supabase/migrations/006_add_cascade_deletes.sql (patrón DROP CONSTRAINT + ADD CONSTRAINT)
    - supabase/migrations/009_sync_subgroup_update_trees.sql (RPC body completo — a recrear con groups)
    - supabase/migrations/010_trees_update_policy.sql (policy UPDATE con plantation_users JOIN)
    - supabase/migrations/011_trees_insert_policy_members.sql (policy INSERT con plantation_users JOIN)
    - .planning/phases/15-schema-migration-data-consolidation/15-PATTERNS.md (sección 012 — orden de 20 statements)
    - .planning/phases/15-schema-migration-data-consolidation/15-RESEARCH.md (Pitfall 2, 3, 4, 7)
  </read_first>
  <action>
Crear `supabase/migrations/012_parcelas_and_rename.sql` con el siguiente contenido completo. **Orden de statements obligatorio** (per 15-PATTERNS.md y Pitfall 7 — el CHECK de estado NO se toca aquí, va en 013 tras el UPDATE):

```sql
-- Migration 012: Parcelas table + rename subgroups→groups + RLS + RPC update
-- Per Phase 15 (v1.1) — applied during coordinated window (D-01).
-- Atomic via BEGIN..COMMIT — Postgres DDL is transactional.
-- Companion: 013_data_consolidation.sql (run AFTER 012 commits successfully).

BEGIN;

-- ============================================================================
-- 1. Crear tabla parcelas (PARC-02, PARC-03)
-- ============================================================================
CREATE TABLE parcelas (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  plantation_id uuid NOT NULL REFERENCES plantations(id) ON DELETE CASCADE,
  nombre text NOT NULL,
  codigo text NOT NULL,
  descripcion text,
  pending_sync boolean NOT NULL DEFAULT false,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  CONSTRAINT parcelas_plantation_nombre_unique UNIQUE (plantation_id, nombre),
  CONSTRAINT parcelas_plantation_codigo_unique UNIQUE (plantation_id, codigo),
  CONSTRAINT parcelas_descripcion_length CHECK (char_length(descripcion) <= 10000)
);

-- ============================================================================
-- 2. Rename subgroups → groups (PARC-04 server side / PARC-05)
-- ============================================================================
ALTER TABLE subgroups RENAME TO groups;

-- 3. FK constraint en groups (rename automático no garantizado en todas las versiones)
ALTER TABLE groups
  DROP CONSTRAINT IF EXISTS subgroups_plantation_id_fkey;
ALTER TABLE groups
  ADD CONSTRAINT groups_plantation_id_fkey
    FOREIGN KEY (plantation_id) REFERENCES plantations(id) ON DELETE CASCADE;

-- ============================================================================
-- 4. Agregar columna parcela_id a groups (PARC-06) — NULL por defecto
-- ============================================================================
ALTER TABLE groups ADD COLUMN parcela_id uuid REFERENCES parcelas(id);

-- ============================================================================
-- 5. Swap unique indexes a per-parcela (PARC-09)
-- ============================================================================
-- DROP unique constraints viejos per-plantation. Postgres puede haber renombrado
-- automáticamente al renombrar la tabla; usamos IF EXISTS para ambos nombres posibles.
ALTER TABLE groups DROP CONSTRAINT IF EXISTS subgroups_plantation_id_codigo_key;
ALTER TABLE groups DROP CONSTRAINT IF EXISTS subgroups_plantation_id_nombre_key;
ALTER TABLE groups DROP CONSTRAINT IF EXISTS groups_plantation_id_codigo_key;
ALTER TABLE groups DROP CONSTRAINT IF EXISTS groups_plantation_id_nombre_key;

CREATE UNIQUE INDEX groups_parcela_codigo_unique ON groups (parcela_id, codigo);
CREATE UNIQUE INDEX groups_parcela_nombre_unique ON groups (parcela_id, nombre);

-- ============================================================================
-- 6. Tipo CHECK: remover 'parcela', agregar 'bosquete' (PARC-07)
-- ============================================================================
ALTER TABLE groups DROP CONSTRAINT IF EXISTS subgroups_tipo_check;
ALTER TABLE groups DROP CONSTRAINT IF EXISTS groups_tipo_check;
ALTER TABLE groups ADD CONSTRAINT groups_tipo_check CHECK (tipo IN ('linea', 'bosquete'));

-- NOTE: estado CHECK ('sincronizada' removal) lives in 013 AFTER the UPDATE
-- normalizacion. Doing it here would violate constraint during the UPDATE
-- (Pitfall 7).

-- ============================================================================
-- 7. Rename trees.subgroup_id → trees.group_id (PARC-05)
-- ============================================================================
ALTER TABLE trees RENAME COLUMN subgroup_id TO group_id;

ALTER TABLE trees DROP CONSTRAINT IF EXISTS trees_subgroup_id_fkey;
ALTER TABLE trees
  ADD CONSTRAINT trees_group_id_fkey
    FOREIGN KEY (group_id) REFERENCES groups(id) ON DELETE CASCADE;

-- ============================================================================
-- 8. RECREAR RLS policies de trees (Pitfall 2 — referencias a subgroups/subgroup_id)
-- ============================================================================
DROP POLICY IF EXISTS "Plantation members can update trees" ON trees;
CREATE POLICY "Plantation members can update trees"
ON trees FOR UPDATE
TO authenticated
USING (
  EXISTS (
    SELECT 1 FROM groups sg
    JOIN plantation_users pu ON pu.plantation_id = sg.plantation_id
    WHERE sg.id = trees.group_id
    AND pu.user_id = auth.uid()
  )
)
WITH CHECK (
  EXISTS (
    SELECT 1 FROM groups sg
    JOIN plantation_users pu ON pu.plantation_id = sg.plantation_id
    WHERE sg.id = trees.group_id
    AND pu.user_id = auth.uid()
  )
);

DROP POLICY IF EXISTS "Plantation members can insert trees" ON trees;
CREATE POLICY "Plantation members can insert trees"
ON trees FOR INSERT
TO authenticated
WITH CHECK (
  EXISTS (
    SELECT 1 FROM groups sg
    JOIN plantation_users pu ON pu.plantation_id = sg.plantation_id
    WHERE sg.id = group_id
    AND pu.user_id = auth.uid()
  )
);

-- ============================================================================
-- 9. RECREAR RPC sync_subgroup (Pitfall 3 — referenciaba `subgroups` y `subgroup_id`)
-- ============================================================================
-- Body identico al de 009 pero con `groups` en lugar de `subgroups` y `group_id`
-- en lugar de `subgroup_id`. Nombre de la función se mantiene `sync_subgroup` para
-- no romper el call site del cliente (Phase 16 lo renombra junto con el cliente).
CREATE OR REPLACE FUNCTION sync_subgroup(
  p_subgroup JSONB,
  p_trees    JSONB
) RETURNS JSONB
LANGUAGE plpgsql SECURITY DEFINER AS $$
BEGIN
  IF EXISTS (
    SELECT 1 FROM groups
    WHERE plantation_id = (p_subgroup->>'plantation_id')::UUID
      AND codigo = p_subgroup->>'codigo'
      AND id <> (p_subgroup->>'id')::UUID
  ) THEN
    RETURN jsonb_build_object('success', false, 'error', 'DUPLICATE_CODE');
  END IF;

  INSERT INTO groups (id, plantation_id, nombre, codigo, tipo, estado, usuario_creador, created_at)
  VALUES (
    (p_subgroup->>'id')::UUID,
    (p_subgroup->>'plantation_id')::UUID,
    p_subgroup->>'nombre',
    p_subgroup->>'codigo',
    p_subgroup->>'tipo',
    'sincronizada',
    (p_subgroup->>'usuario_creador')::UUID,
    (p_subgroup->>'created_at')::TIMESTAMPTZ
  )
  ON CONFLICT (id) DO NOTHING;

  INSERT INTO trees (id, group_id, species_id, posicion, sub_id, foto_url, usuario_registro, created_at)
  SELECT
    (t->>'id')::UUID,
    (t->>'group_id')::UUID,
    NULLIF(t->>'species_id', '')::UUID,
    (t->>'posicion')::INTEGER,
    t->>'sub_id',
    t->>'foto_url',
    (t->>'usuario_registro')::UUID,
    (t->>'created_at')::TIMESTAMPTZ
  FROM jsonb_array_elements(p_trees) AS t
  ON CONFLICT (id) DO UPDATE SET
    species_id = EXCLUDED.species_id,
    sub_id = EXCLUDED.sub_id,
    foto_url = COALESCE(EXCLUDED.foto_url, trees.foto_url);

  RETURN jsonb_build_object('success', true);
EXCEPTION WHEN OTHERS THEN
  RETURN jsonb_build_object('success', false, 'error', 'UNKNOWN');
END;
$$;

GRANT EXECUTE ON FUNCTION sync_subgroup(JSONB, JSONB) TO authenticated;

-- NOTE: el cliente actual (pre-P16) envía `subgroup_id` en el JSON de cada tree.
-- Tras el window, el cliente nuevo (P16) enviará `group_id`. Para soportar el
-- período intermedio NO aplica (D-01: window simultáneo). El body usa `group_id`
-- per la nueva contract documentada en P16.

-- ============================================================================
-- 10. RLS para parcelas (PARC-02)
-- ============================================================================
ALTER TABLE parcelas ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Authenticated users can read parcelas"
  ON parcelas FOR SELECT
  TO authenticated
  USING (true);

CREATE POLICY "Plantation members can insert parcelas"
  ON parcelas FOR INSERT
  TO authenticated
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM plantation_users pu
      WHERE pu.plantation_id = parcelas.plantation_id
      AND pu.user_id = auth.uid()
    )
  );

CREATE POLICY "Plantation members can update parcelas"
  ON parcelas FOR UPDATE
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM plantation_users pu
      WHERE pu.plantation_id = parcelas.plantation_id
      AND pu.user_id = auth.uid()
    )
  )
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM plantation_users pu
      WHERE pu.plantation_id = parcelas.plantation_id
      AND pu.user_id = auth.uid()
    )
  );

CREATE POLICY "Plantation members can delete parcelas"
  ON parcelas FOR DELETE
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM plantation_users pu
      WHERE pu.plantation_id = parcelas.plantation_id
      AND pu.user_id = auth.uid()
    )
  );

COMMIT;
```

**Restricciones críticas:**
- Todo dentro de un único `BEGIN..COMMIT` (D-09 — DDL transaccional Postgres)
- NO incluir `DROP CONSTRAINT groups_estado_check` aquí — eso va en 013 tras `UPDATE estado='finalizada' WHERE estado='sincronizada'` (Pitfall 7)
- DROP de constraints viejos usa IF EXISTS para tolerar variaciones de naming post-RENAME
- Policies INSERT y UPDATE de `parcelas` usan `parcelas.plantation_id` (cualificado) para evitar ambigüedad con la `plantation_id` de `plantation_users`
- RPC mantiene nombre `sync_subgroup` (Phase 16 lo renombra a `sync_group` junto con el call site del cliente)
  </action>
  <verify>
    <automated>test -f supabase/migrations/012_parcelas_and_rename.sql && grep -c "BEGIN;" supabase/migrations/012_parcelas_and_rename.sql | grep -q "^1$" && grep -c "COMMIT;" supabase/migrations/012_parcelas_and_rename.sql | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE parcelas" supabase/migrations/012_parcelas_and_rename.sql` = 1
    - `grep -c "ALTER TABLE subgroups RENAME TO groups" supabase/migrations/012_parcelas_and_rename.sql` = 1
    - `grep -c "RENAME COLUMN subgroup_id TO group_id" supabase/migrations/012_parcelas_and_rename.sql` = 1
    - `grep -c "char_length(descripcion) <= 10000" supabase/migrations/012_parcelas_and_rename.sql` = 1
    - `grep -c "CREATE OR REPLACE FUNCTION sync_subgroup" supabase/migrations/012_parcelas_and_rename.sql` = 1
    - `grep -c "groups_tipo_check" supabase/migrations/012_parcelas_and_rename.sql` ≥ 1
    - `grep -c "linea', 'bosquete" supabase/migrations/012_parcelas_and_rename.sql` = 1
    - `grep -c "FROM groups sg" supabase/migrations/012_parcelas_and_rename.sql` ≥ 2 (UPDATE + INSERT policies recreadas)
    - `grep -c "INSERT INTO groups" supabase/migrations/012_parcelas_and_rename.sql` = 1 (en RPC body)
    - `grep -c "groups_estado_check" supabase/migrations/012_parcelas_and_rename.sql` = 0 (NO tocar estado aquí — Pitfall 7)
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/012_parcelas_and_rename.sql` = 1 (parcelas)
    - Solo un par BEGIN/COMMIT (transacción única)
  </acceptance_criteria>
  <done>
    Archivo SQL versionado, sintácticamente correcto (un único BEGIN..COMMIT), incluye los 10 bloques: parcelas table, rename subgroups→groups, FK groups, parcela_id column, swap unique indexes, tipo CHECK, rename trees column, recreate RLS trees, recreate RPC, RLS parcelas. **NO** modifica el estado CHECK (queda para 013).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear scripts/verify-012.sh (verificación automatizada post-DDL)</name>
  <files>scripts/verify-012.sh</files>
  <read_first>
    - scripts/audit-v1.1-consolidation.sql (estilo de queries SQL contra Supabase)
    - scripts/supabase-backup.sh (cómo se invoca psql con DATABASE_URL en este repo)
    - .planning/phases/15-schema-migration-data-consolidation/15-VALIDATION.md (Wave 0 requirements)
  </read_first>
  <action>
Crear `scripts/verify-012.sh` (ejecutable, bash) que valida cada bloque del DDL aplicado. Estructura:

```bash
#!/usr/bin/env bash
# verify-012.sh — verifica que migration 012_parcelas_and_rename.sql se aplicó correctamente.
# Requiere DATABASE_URL exportado (Supabase Postgres connection string).
# Uso: DATABASE_URL=postgres://... ./scripts/verify-012.sh

set -euo pipefail

if [[ -z "${DATABASE_URL:-}" ]]; then
  echo "ERROR: DATABASE_URL no definido. Exportarlo desde Supabase dashboard."
  exit 1
fi

run_check() {
  local label="$1"
  local sql="$2"
  local expected="$3"
  local actual
  actual=$(psql "$DATABASE_URL" -t -A -c "$sql" 2>&1 | tr -d '[:space:]')
  if [[ "$actual" == "$expected" ]]; then
    echo "OK    | $label (got: $actual)"
  else
    echo "FAIL  | $label (expected: $expected, got: $actual)"
    exit 1
  fi
}

echo "== Verificando migration 012 contra Supabase =="

# 1. Tabla parcelas existe
run_check "parcelas table exists" \
  "SELECT to_regclass('public.parcelas')::text;" \
  "parcelas"

# 2. Tabla groups existe (rename de subgroups)
run_check "groups table exists" \
  "SELECT to_regclass('public.groups')::text;" \
  "groups"

# 3. Tabla subgroups NO existe
run_check "subgroups table dropped" \
  "SELECT COALESCE(to_regclass('public.subgroups')::text, 'NULL');" \
  "NULL"

# 4. groups.parcela_id columna existe
run_check "groups.parcela_id column exists" \
  "SELECT column_name FROM information_schema.columns WHERE table_name='groups' AND column_name='parcela_id';" \
  "parcela_id"

# 5. trees.group_id columna existe (renombrada)
run_check "trees.group_id column exists" \
  "SELECT column_name FROM information_schema.columns WHERE table_name='trees' AND column_name='group_id';" \
  "group_id"

# 6. trees.subgroup_id columna NO existe
run_check "trees.subgroup_id column removed" \
  "SELECT COUNT(*)::text FROM information_schema.columns WHERE table_name='trees' AND column_name='subgroup_id';" \
  "0"

# 7. Unique index per-parcela existe
run_check "groups_parcela_codigo_unique exists" \
  "SELECT indexname FROM pg_indexes WHERE tablename='groups' AND indexname='groups_parcela_codigo_unique';" \
  "groups_parcela_codigo_unique"

# 8. CHECK descripcion <= 10000
run_check "parcelas descripcion CHECK exists" \
  "SELECT conname FROM pg_constraint WHERE conrelid='parcelas'::regclass AND conname='parcelas_descripcion_length';" \
  "parcelas_descripcion_length"

# 9. tipo CHECK acepta bosquete
run_check "groups tipo CHECK includes bosquete" \
  "SELECT (pg_get_constraintdef(oid) LIKE '%bosquete%')::text FROM pg_constraint WHERE conname='groups_tipo_check';" \
  "true"

# 10. RLS habilitada en parcelas
run_check "parcelas RLS enabled" \
  "SELECT relrowsecurity::text FROM pg_class WHERE relname='parcelas';" \
  "t"

# 11. RPC sync_subgroup referencia 'groups' (no 'subgroups')
run_check "sync_subgroup RPC body uses groups" \
  "SELECT (pg_get_functiondef(p.oid) LIKE '%FROM groups%' AND pg_get_functiondef(p.oid) NOT LIKE '%FROM subgroups%')::text FROM pg_proc p WHERE p.proname='sync_subgroup';" \
  "true"

# 12. Policy update trees usa group_id
run_check "trees UPDATE policy uses group_id" \
  "SELECT (qual::text LIKE '%group_id%' AND qual::text NOT LIKE '%subgroup_id%')::text FROM pg_policies WHERE tablename='trees' AND cmd='UPDATE' AND policyname='Plantation members can update trees';" \
  "true"

echo ""
echo "== Todas las verificaciones de 012 pasaron =="
```

Hacer ejecutable:
```bash
chmod +x scripts/verify-012.sh
```

**Restricciones (per CLAUDE.md atomic functions):**
- `run_check` es helper único < 20 líneas
- Cada chequeo es una línea — sin lógica duplicada
- Output simple con prefijo OK/FAIL para grep posterior
  </action>
  <verify>
    <automated>test -x scripts/verify-012.sh && bash -n scripts/verify-012.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/verify-012.sh` (ejecutable)
    - `bash -n scripts/verify-012.sh` (syntax check pasa)
    - `grep -c "run_check" scripts/verify-012.sh` ≥ 12 (cubre los 12 chequeos listados)
    - `grep -c "DATABASE_URL" scripts/verify-012.sh` ≥ 2 (validación + uso)
    - `grep -c "exit 1" scripts/verify-012.sh` ≥ 2 (falla en cada FAIL)
  </acceptance_criteria>
  <done>
    Script ejecutable que falla con exit 1 ante cualquier discrepancia entre el schema esperado y el real. No requiere conectividad para validar — el `bash -n` syntax check es suficiente para CI; la ejecución contra DB se hace en Task 3.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Aplicar 012 en Supabase production + ejecutar verify-012.sh</name>
  <what-built>
    - SQL migration `012_parcelas_and_rename.sql` versionado en `supabase/migrations/`
    - Script `scripts/verify-012.sh` con 12 chequeos automatizados
  </what-built>
  <how-to-verify>
    Per `<schema_push_requirement>` del orchestrator: la DDL Supabase NO se aplica automáticamente — debe ejecutarse manualmente. Build/typecheck del cliente pasa sin que la DDL esté aplicada (los types vienen de schema.ts local), creando false-positive verification. Por eso este checkpoint es **mandatorio**.

    **PRE-CONDICIÓN OBLIGATORIA (D-03):** Antes de ejecutar este checkpoint, confirmar con el equipo:
    - Sync limpio realizado por los 4 técnicos (anuncio explícito en el chat del equipo)
    - Window coordinado activo (D-01)

    **PRE-CONDICIÓN OBLIGATORIA (MIGR-01):** Backup R2 confirmado:
    ```bash
    DATABASE_URL=<supabase-url> ./scripts/supabase-backup.sh
    ```
    Verificar archivo `.dump` subido a R2 + commit del log de backup. Sin backup, NO continuar.

    **Aplicación de la migration:**
    1. Abrir Supabase dashboard → proyecto → SQL Editor → New query
    2. Pegar el contenido completo de `supabase/migrations/012_parcelas_and_rename.sql`
    3. Click "Run"
    4. Esperado: "Success. No rows returned" (la transacción committeó)
    5. Si falla: leer error, hacer ROLLBACK manual no necesario (Postgres ya hizo rollback automático al fallar dentro del BEGIN..COMMIT). Reportar error y abortar window.

    **Verificación:**
    1. Exportar `DATABASE_URL` (desde Supabase dashboard → Settings → Database → Connection string)
    2. Ejecutar:
       ```bash
       DATABASE_URL=<url> ./scripts/verify-012.sh
       ```
    3. Esperado: 12 líneas "OK | ..." y "Todas las verificaciones de 012 pasaron"
    4. Si alguna falla: el ROLLBACK no es posible (la transacción ya committeó); requiere remediation manual o restore desde R2 backup (D-12)

    **Sanity check manual adicional:**
    - Desde Supabase dashboard → Table editor → confirmar que `parcelas` aparece en la lista de tablas
    - Confirmar que `subgroups` NO aparece
    - Click `groups` → ver columna `parcela_id` (uuid, nullable)
    - Click `trees` → ver columna `group_id` (uuid, NOT NULL)
  </how-to-verify>
  <resume-signal>
    Type "applied" después de aplicar 012 + correr `verify-012.sh` con los 12 OK + sanity check de tablas. Reportar cualquier discrepancia o error de la SQL Editor — esto es un punto de no retorno (sin ROLLBACK posible post-COMMIT salvo restore R2). El siguiente paso es Plan 15-03 (data consolidation).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase SQL Editor → Postgres | Migration corre como service_role (bypass RLS) — confiamos en el SQL versionado |
| RPC sync_subgroup → trees + groups | SECURITY DEFINER bypassa RLS pero policies INSERT/UPDATE de trees siguen aplicando para non-RPC paths |
| Authenticated client → parcelas | RLS limita INSERT/UPDATE/DELETE a plantation_users (admin + tecnico asignados) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Elevation of Privilege | RLS de `parcelas` | mitigate | Policies INSERT/UPDATE/DELETE chequean `EXISTS (plantation_users WHERE user_id = auth.uid())` — patrón idéntico a trees policies (010, 011) |
| T-15-06 | Tampering | RPC `sync_subgroup` post-rename | mitigate | `CREATE OR REPLACE FUNCTION` recrea body con `groups`/`group_id` dentro del mismo BEGIN..COMMIT que el rename — atomicidad garantiza consistencia |
| T-15-07 | Denial of Service | Trees RLS post-rename | mitigate | Policies 010 y 011 se DROP+CREATE dentro de la transacción — tras COMMIT, ninguna policy referencia tabla/columna vieja |
| T-15-08 | Information Disclosure | parcelas SELECT policy | accept | `USING (true)` para authenticated — consistente con `species`/`plantations` (no PII en `parcelas`); ASVS L1 V4 satisfecho por auth requirement |
| T-15-09 | Tampering | Input descripcion >10000 chars | mitigate | CHECK constraint server-side `char_length(descripcion) <= 10000` — input validation en V5 ASVS |
</threat_model>

<verification>
- `bash -n scripts/verify-012.sh` syntax check pasa
- `grep -c "BEGIN;" supabase/migrations/012_parcelas_and_rename.sql` = 1 y `grep -c "COMMIT;"` = 1
- Tras aplicar en Supabase: `verify-012.sh` retorna 12 OK + exit 0
- Sanity manual: `parcelas` visible en Table editor, `subgroups` no, `trees.group_id` presente
- Backup R2 confirmado pre-aplicación (per MIGR-01)
</verification>

<success_criteria>
- Migration 012 aplicada en Supabase production atómicamente (per `<schema_push_requirement>`)
- Tabla `parcelas` con CHECK descripcion + RLS (admin + plantation_users) — PARC-02, PARC-03
- `subgroups` renombrada a `groups`; `trees.subgroup_id` renombrada a `trees.group_id` — PARC-04, PARC-05
- `groups.parcela_id` FK NULL agregada — PARC-06
- Unique indexes per-parcela activos — PARC-09
- Tipo CHECK acepta `bosquete`, rechaza `parcela` — PARC-07
- RLS policies de `trees` (010, 011) recreadas con `group_id` — Pitfall 2 mitigado
- RPC `sync_subgroup` body actualizado a `groups`/`group_id` — Pitfall 3 mitigado
- Script `verify-012.sh` retorna 12 OK
</success_criteria>

<output>
After completion, create `.planning/phases/15-schema-migration-data-consolidation/15-02-SUMMARY.md`
</output>
