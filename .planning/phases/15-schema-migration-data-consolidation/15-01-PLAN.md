---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/drizzle/0011_groups_parcelas_migration.sql
  - mobile/drizzle/meta/_journal.json
  - mobile/drizzle/migrations.js
  - mobile/src/database/schema.ts
  - mobile/src/utils/idGenerator.ts
  - mobile/tests/utils/idGenerator.test.ts
  - mobile/tests/database/migrations.test.ts
autonomous: false
requirements:
  - PARC-01
  - PARC-03
  - PARC-04
  - PARC-06
  - PARC-07
  - PARC-08
  - PARC-09
  - PARC-10
  - MIGR-11
tags: [drizzle, sqlite, migration, schema, idgenerator, v1.1]

must_haves:
  truths:
    - "Drizzle migration 0011 ejecuta en app boot sin splash hang en simulador limpio"
    - "Tabla `groups` reemplaza `subgroups` en SQLite local; `trees.group_id` reemplaza `trees.subgrupo_id`"
    - "Tabla `parcelas` existe (vacía) en SQLite local con FK a `plantations`"
    - "Índices únicos de groups son `(parcela_id, codigo)` y `(parcela_id, nombre)` — per-parcela, no per-plantación"
    - "`generateSubId(parcelaCodigo, grupoCodigo, especieCodigo, posicion)` retorna `parcela+grupo+especie+posicion` concatenado"
    - "App compila build/typecheck pasa para `idGenerator.ts` (call sites en repos rompen — aceptado per D-16)"
  artifacts:
    - path: "mobile/drizzle/0011_groups_parcelas_migration.sql"
      provides: "Recreate-table migration: groups, parcelas, trees rename"
      contains: "CREATE TABLE `groups`"
    - path: "mobile/drizzle/meta/_journal.json"
      provides: "Journal entry idx 11"
      contains: "0011_groups_parcelas_migration"
    - path: "mobile/drizzle/migrations.js"
      provides: "m0011 import + registry entry"
      contains: "m0011"
    - path: "mobile/src/database/schema.ts"
      provides: "groups + parcelas tables, trees.group_id"
      contains: "export const groups"
    - path: "mobile/src/utils/idGenerator.ts"
      provides: "Nueva firma 4-arg de generateSubId"
      contains: "parcelaCodigo"
    - path: "mobile/tests/utils/idGenerator.test.ts"
      provides: "Tests actualizados con firma 4-arg"
      contains: "LP1L23BANC12"
    - path: "mobile/tests/database/migrations.test.ts"
      provides: "Smoke test: groups + parcelas existen, subgroups no"
      contains: "groups"
  key_links:
    - from: "mobile/drizzle/migrations.js"
      to: "mobile/drizzle/0011_groups_parcelas_migration.sql"
      via: "import m0011"
      pattern: "m0011"
    - from: "mobile/drizzle/meta/_journal.json"
      to: "mobile/drizzle/0011_groups_parcelas_migration.sql"
      via: "tag entry idx 11"
      pattern: "0011_groups_parcelas_migration"
    - from: "mobile/src/database/schema.ts"
      to: "parcelas (Drizzle table)"
      via: "groups.parcelaId references parcelas.id"
      pattern: "parcela_id"
---

<objective>
Establecer la nueva estructura de schema en SQLite local vía Drizzle migration `0011`: rename `subgroups`→`groups`, agregar tabla `parcelas` (vacía), agregar `groups.parcela_id` FK NULLABLE, swap unique indexes a per-parcela, y cambiar la firma de `generateSubId` a 4 parámetros (`parcelaCodigo, grupoCodigo, especieCodigo, posicion`).

Purpose: Es el primer paso del milestone v1.1. Sin esta migration local, el cliente no puede recibir la nueva estructura del servidor en el primer pull post-window (D-04, D-15). El idGenerator nuevo se usa por el server en el batch UPDATE de SubIDs (Plan 15-03); el cliente lo necesita para futuras inserciones (Phase 16+).

Output: Migration 0011 + schema.ts + idGenerator nuevo + tests, todo aplicado en app boot vía `migrations.js` sin splash hang. **Aceptado:** Los call sites de `idGenerator` en `TreeRepository.ts` y `SubGroupRepository.ts` quedarán rotos hasta Phase 16 (D-16).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-schema-migration-data-consolidation/15-CONTEXT.md
@.planning/phases/15-schema-migration-data-consolidation/15-RESEARCH.md
@.planning/phases/15-schema-migration-data-consolidation/15-PATTERNS.md
@.claude/CLAUDE.md
@mobile/src/database/schema.ts
@mobile/drizzle/migrations.js
@mobile/drizzle/0010_add_tree_conflict_columns.sql
@mobile/drizzle/0000_peaceful_winter_soldier.sql
@mobile/src/utils/idGenerator.ts
@mobile/tests/utils/idGenerator.test.ts

<interfaces>
<!-- Estado actual de schema.ts (a modificar in-place) -->

```typescript
// mobile/src/database/schema.ts (líneas 25-54 actuales)
export const subgroups = sqliteTable('subgroups', {
  id: text('id').primaryKey(),
  plantacionId: text('plantacion_id').notNull().references(() => plantations.id),
  nombre: text('nombre').notNull(),
  codigo: text('codigo').notNull(),
  tipo: text('tipo').notNull().default('linea'),
  estado: text('estado').notNull().default('activa'),
  usuarioCreador: text('usuario_creador').notNull(),
  createdAt: text('created_at').notNull(),
  pendingSync: integer('pending_sync', { mode: 'boolean' }).notNull().default(false),
}, (t) => ({
  uniqueCode: uniqueIndex('subgroups_plantation_code_unique').on(t.plantacionId, t.codigo),
  uniqueName: uniqueIndex('subgroups_plantation_name_unique').on(t.plantacionId, t.nombre),
}));

export const trees = sqliteTable('trees', {
  id: text('id').primaryKey(),
  subgrupoId: text('subgrupo_id').notNull().references(() => subgroups.id),
  especieId: text('especie_id').references(() => species.id),
  posicion: integer('posicion').notNull(),
  subId: text('sub_id').notNull(),
  fotoUrl: text('foto_url'),
  fotoSynced: integer('foto_synced', { mode: 'boolean' }).notNull().default(false),
  plantacionId: integer('plantacion_id'),
  globalId: integer('global_id'),
  usuarioRegistro: text('usuario_registro').notNull(),
  createdAt: text('created_at').notNull(),
  conflictEspecieId: text('conflict_especie_id'),
  conflictEspecieNombre: text('conflict_especie_nombre'),
});
```

<!-- Firma actual idGenerator -->
```typescript
// mobile/src/utils/idGenerator.ts
export function generateSubId(
  subgrupoCodigo: string,
  especieCodigo: string,
  posicion: number
): string {
  return `${subgrupoCodigo}${especieCodigo}${posicion}`;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear migration SQL 0011 + journal + migrations.js (los 3 archivos atómicamente)</name>
  <files>
    mobile/drizzle/0011_groups_parcelas_migration.sql,
    mobile/drizzle/meta/_journal.json,
    mobile/drizzle/migrations.js
  </files>
  <read_first>
    - mobile/drizzle/0000_peaceful_winter_soldier.sql (patrón CREATE TABLE + UNIQUE INDEX + statement-breakpoint)
    - mobile/drizzle/0010_add_tree_conflict_columns.sql (última migration aplicada — referencia idx 10)
    - mobile/drizzle/meta/_journal.json (formato exacto de entries — version "6", when epoch ms)
    - mobile/drizzle/migrations.js (estructura imports + objeto migrations)
    - .planning/phases/15-schema-migration-data-consolidation/15-PATTERNS.md (sección "0011_groups_parcelas_migration.sql" — orden 17 statements)
    - .planning/phases/15-schema-migration-data-consolidation/15-RESEARCH.md (sección "Recreate-Table Pattern")
  </read_first>
  <behavior>
    - Test 1: Smoke test verifica que tras correr migrations en SQLite in-memory, `groups` table existe y `subgroups` no
    - Test 2: Smoke test verifica que `parcelas` table existe
    - Test 3: Smoke test verifica que `trees.group_id` columna existe (no `trees.subgrupo_id`)
    - Test 4: Smoke test verifica que NO ocurre splash hang (migration corre exitosamente)
    - Edge: `parcela_id` en groups es NULLABLE — INSERT con NULL no falla
  </behavior>
  <action>
**1. Crear `mobile/drizzle/0011_groups_parcelas_migration.sql`** con el siguiente recreate-table pattern (usar exactamente este SQL — los `--> statement-breakpoint` son obligatorios entre cada statement):

```sql
CREATE TABLE `parcelas` (
	`id` text PRIMARY KEY NOT NULL,
	`plantacion_id` text NOT NULL,
	`nombre` text NOT NULL,
	`codigo` text NOT NULL,
	`descripcion` text,
	`pending_sync` integer DEFAULT false NOT NULL,
	`created_at` text NOT NULL,
	`updated_at` text NOT NULL,
	FOREIGN KEY (`plantacion_id`) REFERENCES `plantations`(`id`) ON UPDATE no action ON DELETE no action
);
--> statement-breakpoint
CREATE TABLE `groups` (
	`id` text PRIMARY KEY NOT NULL,
	`plantacion_id` text NOT NULL,
	`parcela_id` text,
	`nombre` text NOT NULL,
	`codigo` text NOT NULL,
	`tipo` text DEFAULT 'linea' NOT NULL,
	`estado` text DEFAULT 'activa' NOT NULL,
	`usuario_creador` text NOT NULL,
	`created_at` text NOT NULL,
	`pending_sync` integer DEFAULT false NOT NULL,
	FOREIGN KEY (`plantacion_id`) REFERENCES `plantations`(`id`) ON UPDATE no action ON DELETE no action,
	FOREIGN KEY (`parcela_id`) REFERENCES `parcelas`(`id`) ON UPDATE no action ON DELETE no action
);
--> statement-breakpoint
INSERT INTO `groups` (`id`, `plantacion_id`, `parcela_id`, `nombre`, `codigo`, `tipo`, `estado`, `usuario_creador`, `created_at`, `pending_sync`)
  SELECT `id`, `plantacion_id`, NULL, `nombre`, `codigo`, `tipo`, `estado`, `usuario_creador`, `created_at`, `pending_sync` FROM `subgroups`;
--> statement-breakpoint
CREATE TABLE `trees_new` (
	`id` text PRIMARY KEY NOT NULL,
	`group_id` text NOT NULL,
	`especie_id` text,
	`posicion` integer NOT NULL,
	`sub_id` text NOT NULL,
	`foto_url` text,
	`foto_synced` integer DEFAULT false NOT NULL,
	`plantacion_id` integer,
	`global_id` integer,
	`usuario_registro` text NOT NULL,
	`created_at` text NOT NULL,
	`conflict_especie_id` text,
	`conflict_especie_nombre` text,
	FOREIGN KEY (`group_id`) REFERENCES `groups`(`id`) ON UPDATE no action ON DELETE no action,
	FOREIGN KEY (`especie_id`) REFERENCES `species`(`id`) ON UPDATE no action ON DELETE no action
);
--> statement-breakpoint
INSERT INTO `trees_new` (`id`, `group_id`, `especie_id`, `posicion`, `sub_id`, `foto_url`, `foto_synced`, `plantacion_id`, `global_id`, `usuario_registro`, `created_at`, `conflict_especie_id`, `conflict_especie_nombre`)
  SELECT `id`, `subgrupo_id`, `especie_id`, `posicion`, `sub_id`, `foto_url`, `foto_synced`, `plantacion_id`, `global_id`, `usuario_registro`, `created_at`, `conflict_especie_id`, `conflict_especie_nombre` FROM `trees`;
--> statement-breakpoint
DROP TABLE `trees`;
--> statement-breakpoint
DROP TABLE `subgroups`;
--> statement-breakpoint
ALTER TABLE `trees_new` RENAME TO `trees`;
--> statement-breakpoint
CREATE UNIQUE INDEX `groups_parcela_code_unique` ON `groups` (`parcela_id`,`codigo`);
--> statement-breakpoint
CREATE UNIQUE INDEX `groups_parcela_name_unique` ON `groups` (`parcela_id`,`nombre`);
--> statement-breakpoint
CREATE UNIQUE INDEX `parcelas_plantation_code_unique` ON `parcelas` (`plantacion_id`,`codigo`);
--> statement-breakpoint
CREATE UNIQUE INDEX `parcelas_plantation_name_unique` ON `parcelas` (`plantacion_id`,`nombre`);
```

**2. Actualizar `mobile/drizzle/meta/_journal.json`** — agregar al array `entries`:

```json
{
  "idx": 11,
  "version": "6",
  "when": 1746500000000,
  "tag": "0011_groups_parcelas_migration",
  "breakpoints": true
}
```

**3. Actualizar `mobile/drizzle/migrations.js`** — agregar al final de imports y al objeto:

```javascript
import m0011 from './0011_groups_parcelas_migration.sql';
// ... en migrations: { ...m0010, m0011 }
```

**Restricciones críticas (per Pitfall 1 + lección Phase 13):**
- Los 3 archivos se actualizan ATÓMICAMENTE en un solo commit. Olvidar `migrations.js` = splash hang silencioso.
- `parcela_id` en `groups` debe ser NULLABLE (sin NOT NULL) — Pitfall 5; INSERT SELECT con NULL no falla porque SQLite trata NULLs como distintos en UNIQUE indexes.
- Usar `--> statement-breakpoint` exacto entre cada statement (Drizzle parsea ese marker).
- NO incluir CHECK constraint de `tipo` en SQL local (SQLite es laxo; el constraint vive en Supabase per PARC-07).

**4. Crear `mobile/tests/database/migrations.test.ts`** (smoke test de migration):

```typescript
import { drizzle } from 'drizzle-orm/better-sqlite3';
import { migrate } from 'drizzle-orm/better-sqlite3/migrator';
import Database from 'better-sqlite3';
import path from 'path';

describe('Drizzle migrations bundle', () => {
  it('runs all migrations including 0011 without errors', () => {
    const sqlite = new Database(':memory:');
    const db = drizzle(sqlite);
    expect(() => {
      migrate(db, { migrationsFolder: path.resolve(__dirname, '../../drizzle') });
    }).not.toThrow();
  });

  it('creates groups table and removes subgroups after 0011', () => {
    const sqlite = new Database(':memory:');
    const db = drizzle(sqlite);
    migrate(db, { migrationsFolder: path.resolve(__dirname, '../../drizzle') });
    const tables = sqlite.prepare("SELECT name FROM sqlite_master WHERE type='table'").all() as { name: string }[];
    const names = tables.map((t) => t.name);
    expect(names).toContain('groups');
    expect(names).toContain('parcelas');
    expect(names).not.toContain('subgroups');
  });

  it('creates trees.group_id column (replacing subgrupo_id)', () => {
    const sqlite = new Database(':memory:');
    const db = drizzle(sqlite);
    migrate(db, { migrationsFolder: path.resolve(__dirname, '../../drizzle') });
    const cols = sqlite.prepare("PRAGMA table_info(trees)").all() as { name: string }[];
    const colNames = cols.map((c) => c.name);
    expect(colNames).toContain('group_id');
    expect(colNames).not.toContain('subgrupo_id');
  });
});
```

(per CLAUDE.md rule 9 — tests no contienen lógica de pantallas; este es test de infraestructura puro)
  </action>
  <verify>
    <automated>cd mobile && npm test -- tests/database/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "m0011" mobile/drizzle/migrations.js` ≥ 2 (import + registro)
    - `grep -c "0011_groups_parcelas_migration" mobile/drizzle/meta/_journal.json` = 1
    - `grep -c "CREATE TABLE \`groups\`" mobile/drizzle/0011_groups_parcelas_migration.sql` = 1
    - `grep -c "CREATE TABLE \`parcelas\`" mobile/drizzle/0011_groups_parcelas_migration.sql` = 1
    - `grep -c "DROP TABLE \`subgroups\`" mobile/drizzle/0011_groups_parcelas_migration.sql` = 1
    - `grep -v '^#' mobile/drizzle/0011_groups_parcelas_migration.sql | grep -c "groups_parcela_code_unique"` ≥ 1
    - `npm test -- tests/database/migrations.test.ts` pasa los 3 tests
  </acceptance_criteria>
  <done>
    Los 3 archivos del bundle Drizzle existen y son consistentes; smoke test corre la migration end-to-end en SQLite in-memory; `groups`/`parcelas` existen, `subgroups` no, `trees.group_id` reemplazó `subgrupo_id`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Actualizar schema.ts (groups + parcelas + trees.groupId) e idGenerator (firma 4-arg)</name>
  <files>
    mobile/src/database/schema.ts,
    mobile/src/utils/idGenerator.ts,
    mobile/tests/utils/idGenerator.test.ts
  </files>
  <read_first>
    - mobile/src/database/schema.ts (estructura actual subgroups + trees, líneas 25-54)
    - mobile/src/utils/idGenerator.ts (firma actual 3-arg)
    - mobile/tests/utils/idGenerator.test.ts (4 tests existentes)
    - .planning/phases/15-schema-migration-data-consolidation/15-PATTERNS.md (sección schema.ts y idGenerator.ts — código exacto)
    - .planning/phases/15-schema-migration-data-consolidation/15-RESEARCH.md (D-16 — break aceptado en call sites)
  </read_first>
  <behavior>
    - Test 1: `generateSubId('LP1', 'L23B', 'ANC', 12)` → `'LP1L23BANC12'` (per PARC-08)
    - Test 2: `generateSubId('LP1', 'L23B', 'NN', 5)` → `'LP1L23BNN5'` (N/N tree)
    - Test 3: `generateSubId('MP3', 'L1', 'LAP', 1)` → `'MP3L1LAP1'` (short codes)
    - Test 4: `generateSubId('LP1', 'L23B', 'ANC', 0)` → `'LP1L23BANC0'` (position 0)
    - Test 5: `generateSubId('SO', 'G1', 'ANC', 3)` → `'SOG1ANC3'` (parcela prefix)
    - Edge: cualquier callsite con firma 3-arg compila roto (TypeScript error) — aceptado D-16
  </behavior>
  <action>
**1. Reemplazar `mobile/src/utils/idGenerator.ts` completo** (per PARC-10, D-16 — sin wrapper deprecated):

```typescript
/**
 * Generates the SubID for a tree.
 * Format: {parcelaCodigo}{grupoCodigo}{especieCodigo}{posicion}
 * Examples:
 *   generateSubId('LP1', 'L23B', 'ANC', 12) → 'LP1L23BANC12'
 *   generateSubId('MP3', 'L1', 'NN', 5)    → 'MP3L1NN5'
 *
 * NOTE: Signature changed in Phase 15 (v1.1) — old 3-arg form removed (D-16).
 * Callers in TreeRepository / SubGroupRepository will not compile until Phase 16.
 */
export function generateSubId(
  parcelaCodigo: string,
  grupoCodigo: string,
  especieCodigo: string,
  posicion: number
): string {
  return `${parcelaCodigo}${grupoCodigo}${especieCodigo}${posicion}`;
}
```

**2. Reemplazar `mobile/tests/utils/idGenerator.test.ts` completo** con 5 tests (4 actualizados + 1 nuevo de prefijo parcela):

```typescript
import { generateSubId } from '../../src/utils/idGenerator';

describe('generateSubId', () => {
  it('generates correct SubID for standard tree', () => {
    expect(generateSubId('LP1', 'L23B', 'ANC', 12)).toBe('LP1L23BANC12');
  });

  it('generates correct SubID for N/N tree', () => {
    expect(generateSubId('LP1', 'L23B', 'NN', 5)).toBe('LP1L23BNN5');
  });

  it('generates correct SubID for short codes', () => {
    expect(generateSubId('MP3', 'L1', 'LAP', 1)).toBe('MP3L1LAP1');
  });

  it('handles position 0', () => {
    expect(generateSubId('LP1', 'L23B', 'ANC', 0)).toBe('LP1L23BANC0');
  });

  it('includes parcela code as prefix', () => {
    expect(generateSubId('SO', 'G1', 'ANC', 3)).toBe('SOG1ANC3');
  });
});
```

**3. Actualizar `mobile/src/database/schema.ts`** — reemplazar la export `subgroups` por `groups`, agregar `parcelas`, cambiar `trees.subgrupoId` → `trees.groupId`. El bloque resultante (reemplazo in-place de líneas 25-54) debe quedar:

```typescript
export const parcelas = sqliteTable('parcelas', {
  id: text('id').primaryKey(),
  plantacionId: text('plantacion_id').notNull().references(() => plantations.id),
  nombre: text('nombre').notNull(),
  codigo: text('codigo').notNull(),
  descripcion: text('descripcion'),
  pendingSync: integer('pending_sync', { mode: 'boolean' }).notNull().default(false),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => ({
  uniqueCode: uniqueIndex('parcelas_plantation_code_unique').on(t.plantacionId, t.codigo),
  uniqueName: uniqueIndex('parcelas_plantation_name_unique').on(t.plantacionId, t.nombre),
}));

export const groups = sqliteTable('groups', {
  id: text('id').primaryKey(),
  plantacionId: text('plantacion_id').notNull().references(() => plantations.id),
  parcelaId: text('parcela_id').references(() => parcelas.id),  // NULLABLE per Pitfall 5
  nombre: text('nombre').notNull(),
  codigo: text('codigo').notNull(),
  tipo: text('tipo').notNull().default('linea'),
  estado: text('estado').notNull().default('activa'),
  usuarioCreador: text('usuario_creador').notNull(),
  createdAt: text('created_at').notNull(),
  pendingSync: integer('pending_sync', { mode: 'boolean' }).notNull().default(false),
}, (t) => ({
  uniqueCode: uniqueIndex('groups_parcela_code_unique').on(t.parcelaId, t.codigo),
  uniqueName: uniqueIndex('groups_parcela_name_unique').on(t.parcelaId, t.nombre),
}));

export const trees = sqliteTable('trees', {
  id: text('id').primaryKey(),
  groupId: text('group_id').notNull().references(() => groups.id),  // renombrado de subgrupoId
  especieId: text('especie_id').references(() => species.id),
  posicion: integer('posicion').notNull(),
  subId: text('sub_id').notNull(),
  fotoUrl: text('foto_url'),
  fotoSynced: integer('foto_synced', { mode: 'boolean' }).notNull().default(false),
  plantacionId: integer('plantacion_id'),
  globalId: integer('global_id'),
  usuarioRegistro: text('usuario_registro').notNull(),
  createdAt: text('created_at').notNull(),
  conflictEspecieId: text('conflict_especie_id'),
  conflictEspecieNombre: text('conflict_especie_nombre'),
});
```

**Restricciones (per CLAUDE.md):**
- Funciones < 20 líneas (idGenerator es 1 línea — OK)
- Schema centralizado en schema.ts (no duplicar tables en otros archivos)
- NO mantener wrapper deprecated del idGenerator viejo (D-16 explícito)
- NO renombrar `subgroup_id` legacy en `trees` con columna paralela (D-16 — rename directo)

**Aceptado:** `npx tsc --noEmit` desde mobile/ fallará en `TreeRepository.ts` (4 call sites) y `SubGroupRepository.ts` (línea 237) por firma incompatible — esto es la break intencional de D-16. Los call sites se reparan en Phase 16.
  </action>
  <verify>
    <automated>cd mobile && npm test -- tests/utils/idGenerator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parcelaCodigo" mobile/src/utils/idGenerator.ts` ≥ 1
    - `grep -c "subgrupoCodigo" mobile/src/utils/idGenerator.ts` = 0 (firma vieja eliminada)
    - `grep -c "export const groups" mobile/src/database/schema.ts` = 1
    - `grep -c "export const parcelas" mobile/src/database/schema.ts` = 1
    - `grep -c "export const subgroups" mobile/src/database/schema.ts` = 0
    - `grep -c "groupId: text('group_id')" mobile/src/database/schema.ts` = 1
    - `grep -c "subgrupoId: text('subgrupo_id')" mobile/src/database/schema.ts` = 0
    - `npm test -- tests/utils/idGenerator.test.ts` pasa los 5 tests
  </acceptance_criteria>
  <done>
    schema.ts exporta `groups`, `parcelas`, y `trees` con `groupId`. `subgroups` y `trees.subgrupoId` no existen. idGenerator tiene firma 4-arg con tests verdes. Build de TS romperá en call sites de repos — aceptado per D-16, se repara en P16.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Verificar app boot en simulador limpio sin splash hang</name>
  <what-built>
    - Drizzle migration 0011 (recreate-table pattern: groups, parcelas, trees con group_id)
    - schema.ts con `groups`, `parcelas`, `trees.groupId` (subgroups eliminado)
    - idGenerator con firma 4-arg
    - Tests de migration smoke + tests de idGenerator
  </what-built>
  <how-to-verify>
    Per `<schema_push_requirement>` del orchestrator: las migrations Drizzle se aplican on-boot vía `migrations.js`. Build/typecheck pasa sin aplicar migrations (los types vienen de schema.ts, no de la DB live), creando false-positive verification. Por eso este checkpoint es **mandatorio**.

    1. **Reset simulador a estado limpio:**
       - iOS: `xcrun simctl erase all` (o desde Simulator UI: Device → Erase All Content and Settings)
       - Android: en AVD Manager → Wipe Data
       - Razón: necesitamos un device sin DB previa para que las migrations corran de 0000 a 0011 secuencialmente

    2. **Iniciar app en dev mode:**
       ```bash
       cd mobile && npx expo start --clear
       ```
       Abrir en simulador limpio.

    3. **Verificar app llega a la pantalla de login** (no se queda colgada en splash):
       - Si la pantalla de splash dura > 30 segundos → splash hang → migration falló silenciosamente
       - Si la app crashea con `relation "subgroups" does not exist` → migration corrió pero hay código que aún busca la tabla vieja (esperable hasta P16; reportar pero no bloquear)
       - Si llega al login → migration OK

    4. **Verificar tablas vía Drizzle Studio (opcional, si está disponible) o consola:**
       ```bash
       cd mobile && npx drizzle-kit studio
       ```
       Esperado: tablas `groups`, `parcelas`, `trees` (con `group_id`); NO debe haber `subgroups` ni `trees.subgrupo_id`.

    5. **Verificar que el login NO hace queries a tablas viejas:**
       - El login solo toca `auth` (Supabase) y posiblemente `plantation_users` — no toca `subgroups`/`groups` directo. Si crashea aquí, hay un import suelto.

    6. **Aceptado fallo de TypeScript:** `npx tsc --noEmit` desde mobile/ debe romper en `TreeRepository.ts` y `SubGroupRepository.ts` por la firma nueva de `generateSubId` (D-16). **Esto es esperado**, no bloquea este checkpoint. Reportar como confirmación de que la break intencional ocurrió.
  </how-to-verify>
  <resume-signal>
    Type "approved" si la app llega al login en simulador limpio sin splash hang, o describir el error visto (con logs si hay crash). Si TypeScript rompe SOLO en TreeRepository.ts y SubGroupRepository.ts (sin otros archivos), confirmar como esperado.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App boot → SQLite migrations | Migrations corren con permisos de la app; archivo SQL es read-only embebido en bundle |
| TypeScript compilation → runtime | Break intencional en compile-time previene runtime con firma vieja |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `migrations.js` registry | mitigate | Los 3 archivos (SQL + journal + migrations.js) se commitean atómicamente; CI corre `npm test -- migrations` que falla si la migration no aplica |
| T-15-02 | Denial of Service | App boot (splash hang) | mitigate | Smoke test en `tests/database/migrations.test.ts` corre migration end-to-end en SQLite in-memory; checkpoint humano verifica simulador limpio |
| T-15-03 | Tampering | Local SQLite data residual | accept | Recreate-table pattern preserva data via INSERT SELECT; `parcela_id = NULL` se sobreescribe en primer pull post-migración (D-14, D-15) |
| T-15-04 | Information Disclosure | idGenerator output | accept | Función pura sin secretos; cambio de firma es break compile-time intencional (D-16); riesgo aceptado |
</threat_model>

<verification>
- `npm test -- tests/database/migrations.test.ts` pasa 3 tests
- `npm test -- tests/utils/idGenerator.test.ts` pasa 5 tests
- App boot en simulador limpio llega al login sin splash hang (Task 3 checkpoint)
- `grep -c "m0011" mobile/drizzle/migrations.js` ≥ 2
- `grep -c "subgroups" mobile/src/database/schema.ts` = 0 (rename completo)
- `grep -c "subgrupoCodigo" mobile/src/utils/idGenerator.ts` = 0 (firma vieja eliminada)
- TypeScript rompe SOLO en TreeRepository/SubGroupRepository (D-16 esperado)
</verification>

<success_criteria>
- Drizzle migration 0011 corre en simulador limpio sin splash hang (per `<schema_push_requirement>`)
- Tabla `groups` reemplaza `subgroups`; `parcelas` existe vacía; `trees.group_id` reemplaza `trees.subgrupo_id`
- Índices únicos de `groups` son `(parcela_id, codigo)` y `(parcela_id, nombre)` — per-parcela
- `generateSubId(parcelaCodigo, grupoCodigo, especieCodigo, posicion)` es la única firma exportada
- Tests smoke + tests unitarios verdes
- `migrations.js` actualizado con m0011 (lección Phase 13 respetada)
</success_criteria>

<output>
After completion, create `.planning/phases/15-schema-migration-data-consolidation/15-01-SUMMARY.md`
</output>
