---
phase: 04-admin-export
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - mobile/src/screens/PlantationDetailScreen.tsx
  - mobile/src/screens/AdminScreen.tsx
autonomous: false
requirements: [IDGN-01, IDGN-02, IDGN-03, IDGN-04, EXPO-01, EXPO-02, EXPO-03, PLAN-06]

must_haves:
  truths:
    - "Admin sees 'Generar IDs' button only on finalizada plantations without IDs"
    - "Admin can set seed for global ID with system suggestion of max+1"
    - "IDs are assigned sequentially: plantacionId 1..N, globalId seed..seed+N-1"
    - "Admin sees 'Exportar CSV' and 'Exportar Excel' buttons only after IDs generated"
    - "CSV export contains header row with all 7 required columns"
    - "Excel export produces valid .xlsx file with all 7 required columns"
    - "Export files open native share sheet for saving/sending"
    - "Finalized plantation hides FAB (no new SubGroup creation)"
  artifacts:
    - path: "mobile/src/screens/PlantationDetailScreen.tsx"
      provides: "Admin-only action buttons (ID gen, export) and finalization lockout"
      contains: "generateIds"
    - path: "mobile/src/screens/AdminScreen.tsx"
      provides: "ID gen and export buttons wired (replacing stubs from Plan 02)"
      contains: "exportToCSV"
  key_links:
    - from: "mobile/src/screens/PlantationDetailScreen.tsx"
      to: "mobile/src/repositories/PlantationRepository.ts"
      via: "generateIds call"
      pattern: "generateIds"
    - from: "mobile/src/screens/AdminScreen.tsx"
      to: "mobile/src/services/ExportService.ts"
      via: "exportToCSV and exportToExcel calls"
      pattern: "exportTo(CSV|Excel)"
---

<objective>
Wire ID generation and export functionality into the admin screens, and add finalization lockout to PlantationDetailScreen.

Purpose: This completes the full admin workflow: finalize -> generate IDs -> export. It also ensures technicians cannot create new SubGroups in finalized plantations (Pitfall 8).

Output: Updated PlantationDetailScreen with admin actions and finalization lockout. Updated AdminScreen with working ID gen and export buttons.
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-export/04-CONTEXT.md
@.planning/phases/04-admin-export/04-RESEARCH.md
@.planning/phases/04-admin-export/04-01-SUMMARY.md
@.planning/phases/04-admin-export/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 — data layer -->
From mobile/src/repositories/PlantationRepository.ts:
```typescript
export async function generateIds(plantacionId: string, seed: number): Promise<void>;
```

From mobile/src/queries/adminQueries.ts:
```typescript
export async function getMaxGlobalId(): Promise<number>;
export async function getPlantationEstado(plantacionId: string): Promise<string>;
export async function hasIdsGenerated(plantacionId: string): Promise<boolean>;
```

From mobile/src/services/ExportService.ts:
```typescript
export async function exportToCSV(plantacionId: string, plantationName: string): Promise<void>;
export async function exportToExcel(plantacionId: string, plantationName: string): Promise<void>;
```

<!-- From Plan 02 — AdminScreen already exists -->
From mobile/src/screens/AdminScreen.tsx:
```typescript
// Plantation list with action buttons per estado
// ID gen and export buttons may be stubs — this plan wires them
```

<!-- Existing code -->
From mobile/src/screens/PlantationDetailScreen.tsx:
```typescript
// Shows subgroup list, stats, sync CTA, FAB for new subgroup
// Needs: admin-only action buttons, finalization lockout (hide FAB when finalizada)
```

From mobile/src/hooks/useConfirm.ts:
```typescript
export function useConfirm(): { confirmProps, show, dismiss };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ID generation and export into AdminScreen</name>
  <files>mobile/src/screens/AdminScreen.tsx</files>
  <read_first>
    - mobile/src/screens/AdminScreen.tsx (current state from Plan 02 — find stub buttons)
    - mobile/src/repositories/PlantationRepository.ts (generateIds signature)
    - mobile/src/queries/adminQueries.ts (getMaxGlobalId, hasIdsGenerated)
    - mobile/src/services/ExportService.ts (exportToCSV, exportToExcel)
    - mobile/src/hooks/useConfirm.ts (confirmation pattern for ID gen)
    - mobile/src/components/ConfirmModal.tsx (props interface)
    - mobile/src/theme.ts (style tokens)
  </read_first>
  <action>
    Update AdminScreen.tsx to wire the ID generation and export buttons that were stubs in Plan 02.

    **ID Generation button ("Generar IDs"):**
    - Visible only when plantation estado='finalizada' AND hasIdsGenerated=false
    - On tap: call getMaxGlobalId() to get suggested seed. Show a Modal/prompt with:
      - Text: "Semilla para ID Global"
      - Pre-filled TextInput with value (maxGlobalId + 1).toString()
      - Admin can override the value
      - "Generar" button + "Cancelar" button
    - Before generating: show ConfirmModal: "Se van a generar IDs para {treeCount} arboles. Esta accion no se puede deshacer."
    - On confirm: call generateIds(plantacionId, parseInt(seed))
    - On success: refresh UI (useLiveData handles this via notifyDataChanged)
    - Button color: colors.primary (green — positive action)

    **Export buttons ("Exportar CSV" and "Exportar Excel"):**
    - Visible only when plantation estado='finalizada' AND hasIdsGenerated=true
    - "Exportar CSV" calls exportToCSV(plantacionId, plantation.lugar)
    - "Exportar Excel" calls exportToExcel(plantacionId, plantation.lugar)
    - Show loading indicator during export
    - Buttons in a row: two small buttons side by side
    - Button color: colors.info (blue — informational action)

    **Estado-gated visibility logic per card:**
    ```
    activa: show "Configurar especies", "Asignar tecnicos", "Finalizar"
    finalizada + !hasIds: show "Generar IDs"
    finalizada + hasIds: show "Exportar CSV", "Exportar Excel"
    ```

    For hasIdsGenerated check: query once per plantation when rendering. Use a Map<string, boolean> built from useLiveData that queries all plantations' ID status. OR check per card render if the list is small (<20 plantations). Prefer the simpler approach — check per card.

    Add imports for: generateIds, getMaxGlobalId, hasIdsGenerated, exportToCSV, exportToExcel.

    Seed input: Use a simple Modal with TextInput + keyboardType="number-pad". State: seedModalPlantacionId (string|null), seedValue (string).
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Trabajos/BaykaApp/bayka-app/mobile && npx tsc --noEmit src/screens/AdminScreen.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "generateIds" mobile/src/screens/AdminScreen.tsx returns match
    - grep "getMaxGlobalId" mobile/src/screens/AdminScreen.tsx returns match
    - grep "hasIdsGenerated" mobile/src/screens/AdminScreen.tsx returns match
    - grep "exportToCSV" mobile/src/screens/AdminScreen.tsx returns match
    - grep "exportToExcel" mobile/src/screens/AdminScreen.tsx returns match
    - grep "ConfirmModal" mobile/src/screens/AdminScreen.tsx returns match (confirmation before ID gen)
    - grep "number-pad" mobile/src/screens/AdminScreen.tsx returns match (seed input keyboard)
    - grep "Semilla" mobile/src/screens/AdminScreen.tsx returns match (seed dialog in Spanish)
  </acceptance_criteria>
  <done>AdminScreen has fully working ID generation with seed dialog and export buttons. Action buttons are estado-gated: activa shows management, finalizada shows ID gen, finalizada+IDs shows export.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin actions and finalization lockout to PlantationDetailScreen</name>
  <files>mobile/src/screens/PlantationDetailScreen.tsx</files>
  <read_first>
    - mobile/src/screens/PlantationDetailScreen.tsx (full current file — understand all existing functionality)
    - mobile/src/queries/adminQueries.ts (getPlantationEstado, hasIdsGenerated)
    - mobile/src/repositories/PlantationRepository.ts (generateIds)
    - mobile/src/services/ExportService.ts (exportToCSV, exportToExcel)
    - mobile/src/hooks/useRoutePrefix.ts (to detect admin vs tecnico context)
    - mobile/src/theme.ts (style tokens)
  </read_first>
  <action>
    Modify PlantationDetailScreen.tsx to add:

    **1. Finalization lockout (Pitfall 8):**
    - Query plantation estado via useLiveData(() => getPlantationEstado(pid), [pid])
    - If estado='finalizada': hide the FAB ("+ Nuevo subgrupo" button) entirely
    - Show a banner at the top of fixedHeader (below stats): "Plantacion finalizada" with colors.stateFinalizada background + lock icon
    - This affects BOTH admin and tecnico views — finalized plantations are locked for everyone

    **2. Admin-only action row (only when routePrefix is 'admin'):**
    - Below the finalization banner (or below stats if not finalized), show admin action buttons:
      - If estado='finalizada' AND !hasIdsGenerated: "Generar IDs" button
      - If estado='finalizada' AND hasIdsGenerated: "Exportar CSV" + "Exportar Excel" buttons in a row
    - ID generation: same seed dialog pattern as AdminScreen (Modal with TextInput, getMaxGlobalId suggestion, ConfirmModal before generating)
    - Export: call exportToCSV/exportToExcel with plantacionId and plantation lugar

    **3. Detect admin context:**
    - Use useRoutePrefix() — if it returns 'admin', show admin actions
    - Tecnico users should NOT see ID gen or export buttons

    IMPORTANT: Do NOT break existing functionality. The subgroup list, sync CTA, pull button, N/N banner, edit modal, and ConfirmModal must all continue working exactly as before. Add the new elements in the fixedHeader area, between the existing content and the FlatList.

    Add necessary imports at the top. Keep the StyleSheet additions at the bottom with existing styles.
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Trabajos/BaykaApp/bayka-app/mobile && npx tsc --noEmit src/screens/PlantationDetailScreen.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "getPlantationEstado" mobile/src/screens/PlantationDetailScreen.tsx returns match
    - grep "finalizada" mobile/src/screens/PlantationDetailScreen.tsx returns match (lockout check)
    - grep "generateIds" mobile/src/screens/PlantationDetailScreen.tsx returns match (admin action)
    - grep "exportToCSV" mobile/src/screens/PlantationDetailScreen.tsx returns match (admin action)
    - grep "Plantacion finalizada" mobile/src/screens/PlantationDetailScreen.tsx returns match (lockout banner)
    - grep "hasIdsGenerated" mobile/src/screens/PlantationDetailScreen.tsx returns match (export gate)
    - Existing functionality preserved: grep "startSync" and "SyncProgressModal" still return matches
  </acceptance_criteria>
  <done>PlantationDetailScreen shows finalization lockout banner and hides FAB for finalized plantations. Admin users see ID generation and export action buttons. Existing sync, pull, N/N, and subgroup functionality unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete admin workflow end-to-end</name>
  <files></files>
  <action>Human verification of the complete admin workflow across all 3 plans.</action>
  <what-built>Complete admin workflow: create plantation, configure species, assign technicians, finalize, generate IDs, export CSV/Excel. Plus finalization lockout on detail screen.</what-built>
  <how-to-verify>
    1. Open the app as admin user
    2. Go to Admin tab — verify plantation list shows with estado chips
    3. Tap "Crear plantacion" — fill lugar + periodo, verify plantation appears in list
    4. Tap "Configurar especies" on the new plantation — verify species checklist with up/down arrows
    5. Select some species, reorder, tap "Guardar" — verify saved
    6. Tap "Asignar tecnicos" — verify technician list with toggles
    7. Assign a technician, tap "Guardar" — verify saved
    8. Navigate to plantation detail — verify subgroups can be created
    9. (If possible with test data) Finalize a plantation — verify lockout banner and FAB hidden
    10. (If finalized) Tap "Generar IDs" — verify seed dialog shows suggestion
    11. (If IDs generated) Tap "Exportar CSV" — verify share sheet opens with file
    12. (If IDs generated) Tap "Exportar Excel" — verify share sheet opens with file
  </how-to-verify>
  <verify>Human verification</verify>
  <done>Admin workflow verified end-to-end by user</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Full admin workflow works end-to-end: create -> configure species -> assign technicians -> finalize -> generate IDs -> export
- Finalization lockout prevents SubGroup creation on finalized plantations
- Export files have correct columns: ID Global, ID Parcial, Zona, SubGrupo, SubID, Periodo, Especie
- All buttons are estado-gated (correct actions shown per state)
- Spanish UI language throughout
- No regressions in tecnico workflow
</verification>

<success_criteria>
- Admin can create, configure, assign, finalize, generate IDs, and export — complete lifecycle
- Finalized plantations locked for all users (no new SubGroups)
- CSV and Excel exports contain all 7 required columns
- Share sheet works for both file types
- Existing sync and registration workflows unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-export/04-03-SUMMARY.md`
</output>
