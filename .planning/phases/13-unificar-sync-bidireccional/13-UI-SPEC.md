---
phase: 13
slug: unificar-sync-bidireccional
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-13
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for Phase 13: Unificar sync bidireccional.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual theme via `mobile/src/theme.ts` |
| Preset | not applicable (React Native / Expo, not web) |
| Component library | React Native core + Reanimated |
| Icon library | @expo/vector-icons Ionicons + MaterialCommunityIcons |
| Font | Poppins (body/labels/buttons) + Linux Biolinum (headings) |

**shadcn gate result:** Not applicable. This is a React Native / Expo project. No web design system tooling applies.

**Existing token source:** All tokens read directly from `mobile/src/theme.ts`. Values below are verified against that file — do not re-declare in component files.

---

## Spacing Scale

The project uses a named (non-standard) scale in `theme.ts`. Declared values must reference these tokens only — no hardcoded pixel values in components.

| Token | Value | Usage |
|-------|-------|-------|
| `spacing.xs` | 4 | Icon gaps, dot positioning |
| `spacing.sm` | 6 | Compact element gaps |
| `spacing.md` | 8 | Inner padding (badge, chip) |
| `spacing.lg` | 10 | Button vertical padding, banner inner |
| `spacing.xl` | 12 | Modal gap between elements |
| `spacing.xxl` | 16 | Section padding, card horizontal |
| `spacing.xxxl` | 20 | Modal overlay padding |
| `spacing['4xl']` | 24 | Modal inner padding |
| `spacing['5xl']` | 32 | Bottom sheet bottom padding |
| `spacing['6xl']` | 40 | Major breaks |

Exceptions:
- Orange dot: 8px diameter (hardcoded in `OrangeDot` component as `size` prop default — this is a pixel value, not a spacing token, because it is a fixed visual indicator not a layout gap)
- Count badge: 18px height (existing pattern from `PlantationDetailHeader` `countBadge` — maintain for visual consistency)
- Touch targets: minimum 44px effective touch area via `hitSlop={8}` on small pressables (existing project pattern)

---

## Typography

All sizes reference `fontSize` tokens from `theme.ts`. All weights reference `fonts` tokens.

| Role | Token | Size | Weight Token | Weight | Line Height |
|------|-------|------|-------------|--------|-------------|
| Body / Label | `fontSize.base` | 15px | `fonts.regular` | 400 | 1.4 (implicit RN default) |
| Secondary label | `fontSize.sm` | 12px | `fonts.regular` | 400 | 1.4 |
| Button text | `fontSize.xl` | 16px | `fonts.semiBold` | 600 | 1.0 (button, no wrap) |
| Modal title | `fontSize.xxl` | 18px | `fonts.bold` | 700 | 1.3 |

Typography rules for this phase:
- **OrangeDot** has no typography — it is a pure visual indicator
- **Progress text in SyncProgressModal** uses `fontSize.xl` / `fonts.regular` / `colors.textMuted` (existing `progressText` style — do not change)
- **Phase label in modal** ("Descargando..." / "Subiendo subgrupos...") uses `fontSize.xxl` / `fonts.bold` / `colors.text` (existing `title` style — extend, not replace)
- **Per-plantation progress line** ("Sincronizando [Lugar]... (2 de 5 plantaciones)") uses `fontSize.base` / `fonts.regular` / `colors.textSecondary` — new style, follows existing `currentName` pattern with italic removed

---

## Color

All values sourced from `mobile/src/theme.ts`. No new color constants are allowed except `colors.syncPending` (new addition mandated by D-08).

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `colors.background` | `#FAFAF9` | Screen backgrounds, modal overlays base |
| Secondary (30%) | `colors.surface` | `#FFFFFF` | Cards (PlantationCard, SubGroupCard), modal body, bottom sheet |
| Accent — pull/download actions | `colors.info` | `#2563EB` | Pull phase spinner, Descargar button (if kept as distinct state) |
| Accent — push/primary sync actions | `colors.primary` | `#0A3760` | Unified "Sincronizar" button background, success icon, dismiss button |
| Sync pending dot | `colors.syncPending` | `#F97316` | **NEW — orange-500.** OrangeDot background in ALL placement contexts. Must be added to `theme.ts` in the "State chips" section. |
| Destructive | `colors.danger` | `#DC2626` | Delete confirmation, error messages in SyncProgressModal |
| N/N warning | `colors.secondary` | `#99B95B` | N/N banner, partial sync warning icon |

**Accent reserved for (explicit list):**
- `colors.syncPending` (#F97316): OrangeDot on PlantationCard sidebar, OrangeDot on SubGroupCard row, OrangeDot overlay on global sync icon in PlantacionesScreen header. Used nowhere else.
- `colors.primary` (#0A3760): Unified "Sincronizar" button background, SyncProgressModal push phase spinner, success checkmark icon, dismiss button.
- `colors.info` (#2563EB): SyncProgressModal pull phase spinner only.
- `colors.stateFinalizada` (#F59E0B): Amber is already used for "finalizada" state chip. Must NOT be used for syncPending dot — use #F97316 (orange-500) to maintain contrast distinction.
- `colors.danger` (#DC2626): Failure messages, partial sync failure icon in modal.

**Color not to use:**
- `colors.stateSincronizada` (#0A3760 — brand blue): This was the old "sincronizado" chip color. The chip is being removed (D-10). Do not add a new green/blue "synced" dot. Absence of orange dot = synced.
- `colors.statPending` (#99B95B — olive green): Currently used for "pending" stats. Do not use this for the sync dot. It would clash with stateActiva (same green).

---

## New Token Addition Required

Before implementation, add to `mobile/src/theme.ts` in the `// ─── State chips ──` section:

```typescript
// ─── State chips ──────────────────────────────────────────────────────────────
stateActiva: '#99B95B',
stateFinalizada: '#F59E0B',
stateSincronizada: '#0A3760',   // kept for backward compat during migration
syncPending: '#F97316',         // orange-500 — distinct from stateFinalizada amber
```

This is the single source of truth for the dot color (D-08). All OrangeDot instances read `colors.syncPending`.

---

## Component Inventory

### New Components

#### `OrangeDot` — `mobile/src/components/OrangeDot.tsx`

| Property | Value |
|----------|-------|
| Default size | 8px diameter |
| Shape | Circle (`borderRadius: size / 2`) |
| Color | `colors.syncPending` from theme |
| Position | Absolute overlay, top-right corner of parent (parent must have `position: 'relative'`) |
| Accepts | `size?: number` prop (default 8) |
| Style | `StyleSheet.create` — no inline styles |
| When rendered | Only when `pendingSync === true` for the relevant entity — never render a "synced" version |

**Placement contexts:**

1. **PlantationCard sidebar** — 8px dot absolute over the leaf icon (top: 0, right: 0 within `styles.sidebar`). Show when plantation has any subgroup with `pendingSync = true`.
2. **SubGroupCard row** (`PlantationDetailScreen.renderSubGroup`) — inline 8px dot after the subgroup name, before `SubGroupStateChip`. Show when `item.pendingSync === true`.
3. **Header sync icon** (`PlantacionesScreen`) — 8px dot absolute over the `sync-outline` Ionicons (top: 0, right: 0 within a `position: 'relative'` wrapper View). Show when any local plantation has any subgroup with `pendingSync = true`.

#### `useSyncSetting` — `mobile/src/hooks/useSyncSetting.ts`

No visual output. Manages persistent `incluirFotos` boolean via `SecureStore`. Used by `PlantationDetailHeader` (new unified button) and optionally `PerfilScreen`.

### Modified Components

#### `PlantationDetailHeader` — unified sync button

| Before | After |
|--------|-------|
| Two buttons: "Descargar" (info blue) + "Subir" (primary blue) | One button: "Sincronizar" (primary blue, full width) |
| Two "Incluir fotos" checkboxes | One toggle sourced from `useSyncSetting` (persistent) |
| Calls `onStartPull` + `onStartSync` separately | Calls `onStartSync(incluirFotos)` once — hook handles pull+push |

Button spec:
- Label: "Sincronizar" with `sync-outline` Ionicons (size 18, white)
- Background: `colors.primary` (#0A3760)
- Border radius: `borderRadius.lg` (12px)
- Padding vertical: `spacing.lg` (10px)
- Count badge: shows syncable count (subgroups with `estado='finalizada' AND pendingSync=true`) — badge style same as existing `countBadge` (white bg, 18px height, primary text)
- Disabled state: `opacity: 0.5` when `syncableCount === 0 AND pendingPhotosCount === 0`
- Photo toggle: single `CheckboxRow` with label "Incluir fotos" — reads/writes `useSyncSetting`

#### `SyncProgressModal` — bidirectional phase display

New states added to `SyncState` type:

| State | Spinner Color | Title | Progress Text |
|-------|--------------|-------|--------------|
| `pulling` | `colors.info` (#2563EB) | "Actualizando datos..." | "Descargando novedades del servidor" |
| `pushing` | `colors.primary` (#0A3760) | "Subiendo subgrupos..." | "{completed} de {total}" + `{currentName}` in italic |
| `uploading-photos` | `colors.primary` | "Subiendo fotos..." | "{completed} de {total} fotos" |
| `downloading-photos` | `colors.info` | "Descargando fotos..." | "{completed} de {total} fotos" |
| `done` | icon only | "Sincronizacion completa" / "Sincronizacion parcial" | see existing pattern |

Global sync adds plantation-level progress:
- Below the primary progress text, show: "Sincronizando [Lugar]... ({done} de {total} plantaciones)" using `fontSize.base` / `fonts.regular` / `colors.textSecondary` — follows existing `currentName` style pattern

**`'syncing'` state** is renamed to `'pushing'` in the type. All existing references must be updated.

#### `AdminBottomSheet` — unified sync action

For `plantation.estado === 'activa'` section, add a "Sincronizar" `ActionItem` after "Asignar tecnicos":

```
icon: 'sync-outline'
label: 'Sincronizar'
color: colors.primary
disabled: !isOnline
onPress: onSync (new prop)
helperText: 'Sin conexion' (when offline)
```

The existing `pendingBadge` styling remains unchanged — it already surfaces pending sync state correctly.

Remove the `plantation.estado === 'sincronizada'` section entirely (D-07 removes this state). The export actions (CSV, Excel) remain under `plantation.estado === 'finalizada'` only.

#### `PlantacionesScreen` header — global sync button

Add a new icon button in the header row (right side, after connectivity icon):

```
icon: 'sync-outline' (Ionicons, size 18, white)
wrapper: position: 'relative', padding: spacing.xs
hitSlop: 8
onPress: handleGlobalSync
```

OrangeDot (8px) absolute over the icon when `hasAnyPending === true`.

Button is visible for both admin and tecnico roles (D-03 implies global sync is role-agnostic).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — unified button | "Sincronizar" |
| Primary CTA — global header button | no label (icon only) |
| Photo toggle label | "Incluir fotos" |
| Progress — pull phase | "Actualizando datos..." |
| Progress — push phase | "Subiendo subgrupos..." |
| Progress — per plantation (global sync) | "Sincronizando [Lugar]... ({N} de {M} plantaciones)" |
| Progress — uploading photos | "Subiendo fotos..." |
| Progress — downloading photos | "Descargando fotos..." |
| Done — all success | "Sincronizacion completa" |
| Done — partial failure | "Sincronizacion parcial" |
| Done — pull only success | "Datos actualizados" |
| Done — pull only failure | "Error al actualizar" |
| Error body — connection | "No se pudo conectar con el servidor. Verifica tu conexion." |
| Error body — partial subgroup | "{failureCount} subgrupo{s} con error:" (existing pattern, unchanged) |
| Photo failure message | "{N} foto{s} no pudieron subirse. Podes reintentar en la proxima sincronizacion." (existing, unchanged) |
| Empty state — no pending sync | (no dot shown — absence of dot communicates "todo al dia") |
| Finalize gate blocked | "Para finalizar, todos los subgrupos deben estar sincronizados y finalizados" |
| Bottom sheet pending banner | "Pendiente de sync" (existing, unchanged) |
| Sync disabled helper (offline) | "Sin conexion" |
| Sync disabled helper (no syncable) | none — button is disabled with opacity 0.5 only |
| Dismiss button | "Cerrar" (existing, unchanged) |

**No destructive actions in this phase.** The only deletions are subgroup/plantation deletes which are pre-existing and unchanged. No new confirmation dialogs are introduced.

---

## Interaction States

### OrangeDot — display rules

| Condition | PlantationCard dot | SubGroupCard dot | Header icon dot |
|-----------|-------------------|------------------|-----------------|
| subgroup.pendingSync = true | show (if plantation has any) | show | show (if any plantation has any) |
| subgroup.pendingSync = false, pendingPhotos > 0 | show | show | show |
| all pendingSync = false, no pending photos | hide | hide | hide |

### Unified Sincronizar button — states

| Condition | Visual |
|-----------|--------|
| online, syncable > 0 OR pendingPhotos > 0 | enabled, count badge visible |
| online, syncable = 0, pendingPhotos = 0 | disabled (opacity 0.5), no badge |
| offline | hidden (existing pattern — sync section hidden when `!isOnline`) |

### Global sync icon — states

| Condition | Visual |
|-----------|--------|
| hasAnyPending = true | icon + OrangeDot overlay |
| hasAnyPending = false | icon only (no green variant — absence = clean) |
| sync in progress | icon disabled (opacity 0.5 or press ignored) |

### SyncProgressModal — transition flow

```
idle
  -> [user taps Sincronizar]
  -> pulling (spinner, info blue)
  -> pushing (spinner, primary blue, per-subgroup progress)
  -> uploading-photos (if incluirFotos, spinner, primary blue)
  -> done (checkmark or alert icon, result summary)
  -> [user taps Cerrar]
  -> idle
```

For global sync:
```
pulling (plantation 1 of N)
  -> pushing (plantation 1 of N)
  -> pulling (plantation 2 of N)  [iterates]
  -> pushing (plantation 2 of N)
  -> uploading-photos (all pending, if incluirFotos)
  -> done
```

---

## Accessibility

- All new Pressable elements: `accessibilityLabel` required
  - Global sync button: `accessibilityLabel="Sincronizar todas las plantaciones"`
  - Photo toggle: `accessibilityLabel="Incluir fotos en la sincronizacion"`
  - OrangeDot: not interactive, no label needed (parent Pressable carries context)
- Minimum touch target: 44px effective (via `hitSlop={8}` on small icons)
- Disabled elements: `disabled` prop set + `opacity: 0.5` visual (existing pattern)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — React Native project | not applicable |
| Third-party | none declared | not applicable |

No third-party component registries. All components are handcrafted or from @expo/vector-icons (already installed).

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Orange dot color = #F97316 (`syncPending`) | CONTEXT.md D-08, RESEARCH.md Pattern 6 |
| Orange dot size = 8px | RESEARCH.md Pattern 6 (mirrors existing photo sync dot in TreeRowItem) |
| Dot placement: PlantationCard, SubGroupCard, header icon | CONTEXT.md D-09 |
| Absence of dot = synced (no green dot) | CONTEXT.md D-10 |
| Single "Sincronizar" button replaces Descargar + Subir | CONTEXT.md D-01, D-03 |
| Persistent photo setting via SecureStore | CONTEXT.md D-04, RESEARCH.md Pattern 4 |
| Bidirectional modal phases (pulling/pushing) | RESEARCH.md Pattern 8 |
| Font: Poppins + Linux Biolinum | `theme.ts` — `fonts` object |
| Spacing scale | `theme.ts` — `spacing` object |
| Color palette | `theme.ts` — `colors` object |
| Component library: React Native core | RESEARCH.md Standard Stack |
| Icons: Ionicons | Verified in `PlantationDetailHeader.tsx`, `AdminBottomSheet.tsx` |
| No shadcn / no web design system | Project is Expo React Native |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
