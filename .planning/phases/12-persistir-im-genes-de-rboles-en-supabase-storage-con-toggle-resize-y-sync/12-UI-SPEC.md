---
phase: 12
slug: persistir-imagenes-arboles-supabase-storage-toggle-resize-sync
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-12
---

# Phase 12 — UI Design Contract

> Visual and interaction contract for Phase 12: Persistir imágenes de árboles en Supabase Storage con toggle, resize y sync.
> Generated by gsd-ui-researcher. Consumed by gsd-planner, gsd-executor, gsd-ui-auditor.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — React Native StyleSheet with centralized theme |
| Preset | not applicable |
| Component library | React Native core + Expo components |
| Icon library | @expo/vector-icons (Ionicons) |
| Font | Poppins (body/labels/buttons), Linux Biolinum (headings) |

Source: `mobile/src/theme.ts`, existing component patterns.

---

## Spacing Scale

This project uses a named scale (not strictly 8-point) defined in `mobile/src/theme.ts`. All phase 12 UI must use these tokens exclusively — no hardcoded values.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Hitslop, tight icon gaps |
| sm | 6px | Badge padding, chip vertical padding |
| md | 8px | Row gaps, inner card padding |
| lg | 10px | Button vertical padding, list gaps |
| xl | 12px | Card horizontal padding, button padding |
| xxl | 16px | Section padding, list content padding |
| xxxl | 20px | Modal padding, form sections |
| 4xl | 24px | Card padding, major gaps |
| 5xl | 32px | Screen-level padding |
| 6xl | 40px | Bottom safe area padding |

Exceptions:
- `sm=6px` and `lg=10px` are pre-existing theme tokens in `mobile/src/theme.ts` — they are not multiples of 4 by the 8-point standard, but changing them would require a theme-wide refactor that is out of scope for Phase 12. All phase 12 usage must go through these tokens, never hardcoded.
- Touch targets for checkboxes and photo buttons: minimum 44px tap area via `hitSlop={12}` (matches existing pattern in TreeRow).
- Checkbox + label row: 44px minimum height to meet mobile accessibility guidelines.

---

## Typography

All sizes from `mobile/src/theme.ts` fontSize tokens. No hardcoded values.

**Maximum 2 font weights in this phase: 400 (body/caption) and 600 (labels/buttons/modal titles).**

| Role | Token | Size | Weight / Font | Line Height |
|------|-------|------|---------------|-------------|
| Body | `fontSize.base` | 15px | regular (400) / Poppins_400Regular | 1.5 |
| Label / button | `fontSize.xl` | 16px | semiBold (600) / Poppins_600SemiBold | 1.4 |
| Caption / subtext | `fontSize.sm` | 12px | regular (400) / Poppins_400Regular | 1.4 |
| Modal title | `fontSize.xxl` | 18px | semiBold (600) / Poppins_600SemiBold | 1.2 |

Note: Modal title uses `semiBold (600)` — same as labels and buttons. Bold (700) is not used in this phase. This consolidates the scale to 2 weights.

Source: derived from existing SyncProgressModal and PlantationDetailScreen patterns.

---

## Color

All values from `mobile/src/theme.ts`. Never hardcode hex values in component files.

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `colors.background` / `colors.surface` | #FAFAF9 / #FFFFFF | Screen background, modal surface |
| Secondary (30%) | `colors.backgroundAlt` / `colors.primaryBg` | #F5F5F4 / #EDF4F9 | Sync button row background, toggle row container |
| Accent (10%) | `colors.info` | #2563EB | Descargar button (pull action) |
| Push action | `colors.primary` | #0A3760 | Subir button (push action) — matches existing sync CTA pattern |
| Destructive | `colors.danger` | #DC2626 | Error states, failure messages only |

Accent (`colors.info`) reserved for:
- The "Descargar" pull button background
- The ActivityIndicator during pulling state (matches existing `SyncProgressModal` pulling state)
- Photo download progress counter text

`colors.primary` (#0A3760) reserved for:
- The "Subir" push button background
- The ActivityIndicator during syncing/uploading state
- Success checkmark icon in done state

`colors.secondary` (#99B95B / olive green) reserved for:
- Warning icon when partial failures occur (matches existing SyncProgressModal)
- `fotoSynced=true` indicator badge on TreeRow

`colors.stateFinalizada` (#F59E0B / amber) reserved for:
- Unsynced photo count badge on TreeRow when `fotoUrl != null AND fotoSynced = false`

---

## Component Inventory

### Modified Components

**SyncProgressModal** (`mobile/src/components/SyncProgressModal.tsx`)
- Add two new `state` values: `'uploading-photos'` and `'downloading-photos'`
- `uploading-photos` state: ActivityIndicator with `colors.primary`, title "Subiendo fotos...", progress text "N de M fotos"
- `downloading-photos` state: ActivityIndicator with `colors.info`, title "Descargando fotos...", progress text "N de M fotos"
- Both photo states follow the identical layout as the existing `syncing` / `pulling` states
- Modal title uses `semiBold (600) / Poppins_600SemiBold` at `fontSize.xxl` (18px)

**PlantationDetailHeader** (`mobile/src/components/PlantationDetailHeader.tsx`)
- Replace single sync CTA area with two-button row: **"Descargar"** (left) and **"Subir"** (right)
- Each button occupies 50% width of the row with `gap: spacing.md` between them
- Below each button: a `CheckboxRow` component with label "Incluir fotos"
- Entire sync section is disabled when offline (same as current behavior)
- Visual hierarchy: the two buttons carry equal visual weight — neither is primary over the other. They are differentiated by color only (`colors.info` for Descargar, `colors.primary` for Subir). No size, elevation, or shadow difference between them.

**TreeRow** (`mobile/src/components/TreeRow.tsx`)
- Photo indicator: replace emoji icons with Ionicons — `image-outline` (no photo) and `image` (has local photo)
- Add sync badge: when `fotoUrl != null AND fotoSynced = false`, show a small amber dot (`colors.stateFinalizada`) overlaid on the photo icon (top-right corner, 8px circle)
- When `fotoSynced = true`, show a small blue dot (`colors.statSynced`) overlaid on the photo icon
- `accessibilityLabel` values for the photo icon touchable:
  - No photo: `"Sin foto"`
  - Has photo, not synced: `"Foto pendiente de subida"`
  - Has photo, synced: `"Foto sincronizada"`

### New Components

**CheckboxRow** (new — `mobile/src/components/CheckboxRow.tsx`)
- Layout: `flexDirection: 'row'`, `alignItems: 'center'`, `gap: spacing.md`
- Checkbox: 20px × 20px, `borderRadius: borderRadius.sm`, `borderWidth: 1.5`, `borderColor: colors.border`
- Checked state: filled with `colors.primary`, checkmark icon (Ionicons `checkmark` size 14, white)
- Unchecked state: transparent fill, border `colors.border`
- Label: `fontSize.sm` (12px), `fonts.regular`, `colors.textSecondary`
- Always starts checked (ON by default per D-08, D-09)
- Minimum touch area: 44px height on the row container

---

## Interaction Contracts

### Sync Button Row (PlantationDetailHeader)

```
[ Descargar ]  [ Subir ]
[x] Incluir fotos  [x] Incluir fotos
```

- Both buttons: `flex: 1`, `paddingVertical: spacing.lg` (10px), `borderRadius: borderRadius.lg`
- "Descargar" background: `colors.info` (#2563EB), text: `colors.white`
- "Subir" background: `colors.primary` (#0A3760), text: `colors.white`
- Equal visual weight: same size, same padding, same border radius, same font size and weight. Color alone distinguishes them.
- Both disabled (opacity 0.5) when offline
- "Subir" additionally disabled when `syncableCount === 0`
- "Subir" shows count badge: `{syncableCount}` in a small circle (12px) top-right of button when count > 0
- CheckboxRow defaults to checked; user can uncheck to skip photos for that action

### Photo Indicator on TreeRow

- Icon: Ionicons `image-outline` (no local photo) or `image` (has local photo), size 18, color `colors.primaryAccent`
- `accessibilityLabel` on the touchable wrapping the icon:
  - `"Sin foto"` when no photo
  - `"Foto pendiente de subida"` when `fotoUrl != null AND fotoSynced = false`
  - `"Foto sincronizada"` when `fotoSynced = true`
- Sync state dot: 8px circle, positioned absolute top-right of photo icon
  - Pending upload: `colors.stateFinalizada` (#F59E0B)
  - Synced: `colors.statSynced` (#0A3760)
  - No photo: no dot shown
- Tap opens PhotoViewerModal if photo exists; opens picker if no photo (existing behavior unchanged)
- Minimum tap area: `hitSlop={12}` (matches current TreeRow pattern)

### Progress States in SyncProgressModal

| State | Spinner color | Title | Progress text |
|-------|--------------|-------|---------------|
| `syncing` | `colors.primary` | "Sincronizando..." | "N de M" (existing) |
| `pulling` | `colors.info` | "Actualizando datos..." | "Descargando novedades del servidor" (existing) |
| `uploading-photos` | `colors.primary` | "Subiendo fotos..." | "N de M fotos" |
| `downloading-photos` | `colors.info` | "Descargando fotos..." | "N de M fotos" |

Photo states appear sequentially after their respective SubGroup sync phases — not interleaved.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Pull CTA button | "Descargar" |
| Push CTA button | "Subir" |
| Photo toggle label | "Incluir fotos" |
| Uploading photos progress | "Subiendo fotos... N de M" |
| Downloading photos progress | "Descargando fotos... N de M" |
| Photo upload success (partial) | "N fotos subidas correctamente" |
| Photo upload error (single) | "No se pudo subir la foto del árbol {subId}. Podés reintentar en la próxima sincronización." |
| Photo upload error (multiple) | "N fotos no pudieron subirse. Podés reintentar en la próxima sincronización." |
| Photo download error | "Algunas fotos no pudieron descargarse. Verificá tu conexión e intentá de nuevo." |
| Empty state (no subgroups) | "No hay subgrupos aun" / "Toca '+' para crear el primero" (unchanged — existing copy) |
| Push disabled (offline) | Button disabled, no toast — consistent with existing offline handling |
| Push disabled (no syncable) | "Subir" button disabled with `opacity: 0.5`, no explanation needed (count badge shows 0) |

Source: D-17 (Spanish progress messages), existing SyncProgressModal copy patterns, CONTEXT.md specifics section.

No destructive actions introduced in this phase. Photo sync failures are recoverable (retry on next sync) — no confirmation dialogs required.

---

## States and Feedback

### TreeRow photo icon states

| Condition | Icon | Dot color | Accessibility label | Tap action |
|-----------|------|-----------|---------------------|------------|
| No photo | `image-outline` | none | "Sin foto" | Open picker |
| Has photo, not synced | `image` | `colors.stateFinalizada` (amber) | "Foto pendiente de subida" | Open PhotoViewerModal |
| Has photo, synced | `image` | `colors.statSynced` (dark blue) | "Foto sincronizada" | Open PhotoViewerModal |

### Sync button states

| Condition | Descargar | Subir |
|-----------|-----------|-------|
| Online, has syncable subgroups | Enabled (info blue) | Enabled (primary blue) |
| Online, no syncable subgroups | Enabled | Disabled (opacity 0.5) |
| Offline | Disabled (opacity 0.5) | Disabled (opacity 0.5) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| No shadcn | none | not applicable — React Native project |

No third-party registries. Image resizing will use `expo-image-manipulator` (already in Expo SDK ecosystem — no additional registry needed).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
