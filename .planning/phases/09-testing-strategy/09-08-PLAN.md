---
phase: 09-testing-strategy
plan: "08"
type: execute
wave: 1
depends_on: ["09-03", "09-06"]
files_modified:
  - mobile/src/screens/PlantationDetailScreen.tsx
autonomous: true
requirements: [TEST-REFACTOR]
gap_closure: true

must_haves:
  truths:
    - "PlantationDetailScreen imports usePlantationDetail — zero imports from repositories/, queries/ (except type re-exports via hook)"
    - "PlantationDetailScreen is under 350 lines"
    - "All three testIDs (subgroup-card-*, sync-button, subgroup-list) are present for Maestro E2E flows"
  artifacts:
    - path: "mobile/src/screens/PlantationDetailScreen.tsx"
      provides: "Thin composition screen using usePlantationDetail hook"
      max_lines: 350
  key_links:
    - from: "mobile/src/screens/PlantationDetailScreen.tsx"
      to: "mobile/src/hooks/usePlantationDetail.ts"
      via: "import { usePlantationDetail }"
      pattern: "import.*usePlantationDetail.*from.*hooks/usePlantationDetail"
---

<objective>
Restore the PlantationDetailScreen refactor that was overwritten by commit 2436f72 (testIDs commit). The refactored version existed at commit 4463c76 (199 lines, using usePlantationDetail hook) but was overwritten. This plan restores the refactored screen AND preserves testIDs needed by Maestro E2E flows.

Purpose: Close Blocker 2 from VERIFICATION.md — PlantationDetailScreen must comply with CLAUDE.md rule 9 and be under 350 lines.
Output: PlantationDetailScreen.tsx refactored to use usePlantationDetail hook, under 350 lines, with testIDs preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-strategy/09-03-SUMMARY.md
@.planning/phases/09-testing-strategy/09-06-SUMMARY.md

Root cause: Same as Blocker 1 — commit 2436f72 (feat(09-06): add testIDs) overwrote the refactored PlantationDetailScreen from 4463c76. The usePlantationDetail hook and PlantationDetailHeader component exist and are functional. Only the screen file needs restoration.

The refactored version exists in git at commit 4463c76. Use `git show 4463c76:mobile/src/screens/PlantationDetailScreen.tsx` as the base, then ensure testIDs are present.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore refactored PlantationDetailScreen with testIDs</name>
  <files>mobile/src/screens/PlantationDetailScreen.tsx</files>
  <read_first>
    mobile/src/screens/PlantationDetailScreen.tsx
    mobile/src/hooks/usePlantationDetail.ts
    mobile/src/components/PlantationDetailHeader.tsx
  </read_first>
  <action>
    Restore the refactored PlantationDetailScreen from git commit 4463c76 and ensure testIDs are present.

    Step 1: Run `git show 4463c76:mobile/src/screens/PlantationDetailScreen.tsx` to get the refactored 199-line version. This is the base.

    Step 2: Write that content to mobile/src/screens/PlantationDetailScreen.tsx.

    Step 3: Verify the three testIDs needed by Maestro E2E flows are present:
    - `testID={`subgroup-card-${item.id}`}` on the subgroup card Pressable (in renderSubGroup)
    - `testID="sync-button"` on the sync Pressable (likely in PlantationDetailHeader.tsx component now)
    - `testID="subgroup-list"` on the FlatList

    IMPORTANT: Some testIDs may need to go into PlantationDetailHeader.tsx since the sync button was extracted there. Check PlantationDetailHeader.tsx and add testID="sync-button" on the sync Pressable if missing.

    Step 4: Verify the screen has ZERO imports from repositories/ or direct query function imports. The only data imports should be via usePlantationDetail hook. Types imported from the hook (SubGroup, SubGroupTipo) are acceptable since usePlantationDetail re-exports them.

    Step 5: The refactored version imports `getPlantationEstado` indirectly through `usePlantationDetail` — verify the screen does NOT have `import { getPlantationEstado } from '../queries/adminQueries'`.

    Step 6: Verify the screen is under 350 lines.

    Files that may also need testID additions:
    - mobile/src/components/PlantationDetailHeader.tsx — add testID="sync-button" on sync Pressable if not present
  </action>
  <verify>
    <automated>cd mobile && wc -l src/screens/PlantationDetailScreen.tsx && grep -c "import.*from.*repositories/\|import.*from.*queries/" src/screens/PlantationDetailScreen.tsx; grep "testID" src/screens/PlantationDetailScreen.tsx src/components/PlantationDetailHeader.tsx; npx jest --ci --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l mobile/src/screens/PlantationDetailScreen.tsx` outputs a number less than 350
    - `grep -c "import.*from.*repositories/\|import.*from.*queries/" mobile/src/screens/PlantationDetailScreen.tsx` outputs 0
    - `grep "usePlantationDetail" mobile/src/screens/PlantationDetailScreen.tsx` returns a match
    - `grep 'testID="subgroup-list"' mobile/src/screens/PlantationDetailScreen.tsx` returns a match
    - `grep 'testID=.*subgroup-card' mobile/src/screens/PlantationDetailScreen.tsx` returns a match
    - `grep 'testID="sync-button"' mobile/src/screens/PlantationDetailScreen.tsx mobile/src/components/PlantationDetailHeader.tsx` returns a match in at least one file
    - `grep "getPlantationEstado" mobile/src/screens/PlantationDetailScreen.tsx` returns NO match (it should be in the hook, not the screen)
  </acceptance_criteria>
  <done>PlantationDetailScreen.tsx is under 350 lines, uses usePlantationDetail hook, has zero direct repository/query imports, and all Maestro testIDs are preserved.</done>
</task>

</tasks>

<verification>
- `wc -l mobile/src/screens/PlantationDetailScreen.tsx` less than 350
- `grep -c "import.*from '.*repositories/\|import.*from '.*queries/" mobile/src/screens/PlantationDetailScreen.tsx` equals 0
- testIDs preserved for Maestro E2E flows
- `cd mobile && npx jest --ci --no-coverage` passes (no regressions)
</verification>

<success_criteria>
PlantationDetailScreen.tsx is a thin composition layer under 350 lines with zero CLAUDE.md rule 9 violations and all Maestro testIDs preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-strategy/09-08-SUMMARY.md`
</output>
