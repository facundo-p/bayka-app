---
phase: 09-testing-strategy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/screens/TreeRegistrationScreen.tsx
  - mobile/src/hooks/useTreeRegistration.ts
  - mobile/src/hooks/useSpeciesOrder.ts
  - mobile/src/hooks/useNNFlow.ts
  - mobile/src/components/SpeciesGrid.tsx
  - mobile/src/components/LastThreeTrees.tsx
  - mobile/src/components/TreeRegistrationHeader.tsx
autonomous: true
requirements:
  - TEST-REFACTOR

must_haves:
  truths:
    - "TreeRegistrationScreen.tsx is under 300 lines"
    - "TreeRegistrationScreen has zero direct imports from repositories/queries/services"
    - "Tree registration behavior is unchanged after refactor (species tap, undo, N/N, reverse, finalize)"
  artifacts:
    - path: "mobile/src/hooks/useTreeRegistration.ts"
      provides: "Tree CRUD logic: addTree, undoLastTree, reverseTrees, finalize"
      min_lines: 50
    - path: "mobile/src/hooks/useSpeciesOrder.ts"
      provides: "Species ordering, user species order persistence"
      min_lines: 30
    - path: "mobile/src/hooks/useNNFlow.ts"
      provides: "N/N tree photo capture and resolution state"
      min_lines: 30
    - path: "mobile/src/components/SpeciesGrid.tsx"
      provides: "Species button grid component"
      min_lines: 30
    - path: "mobile/src/components/LastThreeTrees.tsx"
      provides: "Last 3 registered trees display"
      min_lines: 20
    - path: "mobile/src/components/TreeRegistrationHeader.tsx"
      provides: "Registration screen header with counts"
      min_lines: 20
  key_links:
    - from: "mobile/src/screens/TreeRegistrationScreen.tsx"
      to: "mobile/src/hooks/useTreeRegistration.ts"
      via: "hook import"
      pattern: "import.*useTreeRegistration"
    - from: "mobile/src/hooks/useTreeRegistration.ts"
      to: "mobile/src/repositories/TreeRepository.ts"
      via: "repository call"
      pattern: "import.*TreeRepository"
---

<objective>
Decompose TreeRegistrationScreen.tsx from 1054 lines into focused hooks and components, achieving under 300 lines per D-01/D-02.

Purpose: Make the largest screen file testable by extracting business logic into hooks (per CLAUDE.md rule 9) and UI into reusable components. The screen becomes a thin composition layer.
Output: 3 new hooks, 3 new components, TreeRegistrationScreen under 300 lines with zero direct data imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-strategy/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract hooks from TreeRegistrationScreen</name>
  <files>
    mobile/src/hooks/useTreeRegistration.ts,
    mobile/src/hooks/useSpeciesOrder.ts,
    mobile/src/hooks/useNNFlow.ts
  </files>
  <read_first>
    mobile/src/screens/TreeRegistrationScreen.tsx,
    mobile/src/repositories/TreeRepository.ts,
    mobile/src/repositories/SubGroupRepository.ts,
    mobile/src/repositories/UserSpeciesOrderRepository.ts,
    mobile/src/services/PhotoService.ts,
    mobile/src/queries/plantationDetailQueries.ts,
    mobile/src/hooks/useTrees.ts,
    mobile/src/hooks/usePlantationSpecies.ts,
    mobile/src/hooks/usePhotoPicker.ts
  </read_first>
  <action>
Read TreeRegistrationScreen.tsx completely. Identify three logical groups of state and functions:

**Hook 1: useTreeRegistration.ts**
Extract all tree CRUD logic:
- State: current subgroup data, finalization state
- Functions: `addTree(especieId, especieCodigo)`, `undoLastTree()`, `reverseTrees()`, `finalizeSubGroup()`
- Import from: `TreeRepository` (createTree, deleteLastTree), `SubGroupRepository` (finalizeSubGroup, updateSubGroupCode), `plantationDetailQueries` (getSubgroupById)
- Accept parameters: `{ subgrupoId, plantacionId, subgrupoCodigo }` from route params
- Return: `{ trees, treeCount, subgroupEstado, addTree, undoLastTree, reverseTrees, finalizeSubGroup, isLoading }`
- Use existing `useTrees` hook for reactive tree list

**Hook 2: useSpeciesOrder.ts**
Extract species ordering logic:
- State: ordered species list, drag state
- Functions: `moveSpeciesUp(index)`, `moveSpeciesDown(index)`, `saveOrder()`
- Import from: `UserSpeciesOrderRepository` (saveUserSpeciesOrder), `usePlantationSpecies`
- Accept parameters: `{ plantacionId, userId }`
- Return: `{ orderedSpecies, moveSpeciesUp, moveSpeciesDown, hasCustomOrder }`

**Hook 3: useNNFlow.ts**
Extract N/N tree registration flow:
- State: NN modal visible, pending photo URI, selected species for NN
- Functions: `startNNCapture()`, `captureNNPhoto(uri)`, `confirmNN(especieId)`
- Import from: `PhotoService` (launchCamera, launchGallery), `TreeRepository` (createTree)
- Accept parameters: `{ subgrupoId, subgrupoCodigo }`
- Return: `{ isNNModalVisible, nnPhotoUri, startNNCapture, captureNNPhoto, confirmNN, cancelNN }`

Each hook must:
- Import data layer modules (repositories/queries/services) — NOT the screen
- Use TypeScript types properly (import SubGroupEstado, etc.)
- Follow CLAUDE.md: no inline SQL, functions under 20 lines, import colors from theme
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Trabajos/BaykaApp/bayka-app-redesign/mobile && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/src/hooks/useTreeRegistration.ts` exists and exports `useTreeRegistration`
    - `mobile/src/hooks/useSpeciesOrder.ts` exists and exports `useSpeciesOrder`
    - `mobile/src/hooks/useNNFlow.ts` exists and exports `useNNFlow`
    - `useTreeRegistration.ts` contains `import.*TreeRepository` (grep confirms)
    - `useSpeciesOrder.ts` contains `import.*UserSpeciesOrderRepository` (grep confirms)
    - `useNNFlow.ts` contains `import.*PhotoService` (grep confirms)
    - `npx tsc --noEmit` produces zero errors
  </acceptance_criteria>
  <done>Three hooks extracted with all business logic from TreeRegistrationScreen. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Extract components + slim down TreeRegistrationScreen</name>
  <files>
    mobile/src/components/SpeciesGrid.tsx,
    mobile/src/components/LastThreeTrees.tsx,
    mobile/src/components/TreeRegistrationHeader.tsx,
    mobile/src/screens/TreeRegistrationScreen.tsx
  </files>
  <read_first>
    mobile/src/screens/TreeRegistrationScreen.tsx,
    mobile/src/hooks/useTreeRegistration.ts,
    mobile/src/hooks/useSpeciesOrder.ts,
    mobile/src/hooks/useNNFlow.ts,
    mobile/src/theme.ts
  </read_first>
  <action>
**Step 1: Extract SpeciesGrid.tsx**

Create a presentational component that renders the species button grid. Props:
```typescript
interface SpeciesGridProps {
  species: Array<{ id: string; codigo: string; nombre: string }>;
  onSpeciesTap: (especieId: string, especieCodigo: string) => void;
  onNNTap: () => void;
  disabled: boolean;
}
```
Move the grid layout JSX and styles from TreeRegistrationScreen. Import colors from `src/theme.ts`.

**Step 2: Extract LastThreeTrees.tsx**

Presentational component for the last 3 registered trees display. Props:
```typescript
interface LastThreeTreesProps {
  trees: Array<{ id: string; subId: string; especieId: string | null; posicion: number }>;
  speciesMap: Record<string, string>;  // especieId -> nombre
}
```
Move the last-3 tree rendering JSX and styles from TreeRegistrationScreen.

**Step 3: Extract TreeRegistrationHeader.tsx**

Header component showing subgroup info and tree count. Props:
```typescript
interface TreeRegistrationHeaderProps {
  subgroupName: string;
  subgroupCodigo: string;
  treeCount: number;
  estado: string;
}
```

**Step 4: Rewrite TreeRegistrationScreen.tsx**

The screen becomes a thin composition layer that:
1. Reads route params (`subgrupoId`, `plantacionId`, `subgrupoCodigo`)
2. Calls the 3 extracted hooks
3. Renders the 3 extracted components + action buttons (undo, reverse, finalize)
4. Has ZERO imports from `repositories/`, `queries/`, `services/`, or `database/`

Target: under 300 lines. The screen should only import from:
- `react` / `react-native`
- `expo-router` (for route params)
- `../hooks/useTreeRegistration`
- `../hooks/useSpeciesOrder`
- `../hooks/useNNFlow`
- `../hooks/usePhotoPicker` (if modal still needed in screen)
- `../components/SpeciesGrid`
- `../components/LastThreeTrees`
- `../components/TreeRegistrationHeader`
- `../theme`

Ensure ALL existing behavior is preserved:
- One-tap species registration
- N/N flow with mandatory photo
- Undo last tree
- Reverse order
- Finalize subgroup
- Read-only mode when estado === 'sincronizada'
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Trabajos/BaykaApp/bayka-app-redesign && wc -l mobile/src/screens/TreeRegistrationScreen.tsx && npx --prefix mobile tsc --noEmit 2>&1 | tail -5 && grep -c "import.*from.*repositories\|import.*from.*queries\|import.*from.*services\|import.*from.*database" mobile/src/screens/TreeRegistrationScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l mobile/src/screens/TreeRegistrationScreen.tsx` shows less than 300
    - `grep -c "import.*from.*repositories\|import.*from.*queries\|import.*from.*services\|import.*from.*database" mobile/src/screens/TreeRegistrationScreen.tsx` shows 0
    - `mobile/src/components/SpeciesGrid.tsx` exists
    - `mobile/src/components/LastThreeTrees.tsx` exists
    - `mobile/src/components/TreeRegistrationHeader.tsx` exists
    - `npx tsc --noEmit` produces zero errors
    - `npx jest --no-coverage` all existing tests still pass
  </acceptance_criteria>
  <done>TreeRegistrationScreen is under 300 lines with zero direct data imports. Three presentational components extracted. All existing tests pass. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `wc -l mobile/src/screens/TreeRegistrationScreen.tsx` < 300
- `grep -r "import.*from.*repositories\|import.*from.*queries\|import.*from.*services\|import.*from.*database" mobile/src/screens/TreeRegistrationScreen.tsx` returns empty
- `cd mobile && npx tsc --noEmit` — zero errors
- `cd mobile && npx jest --no-coverage` — all existing tests pass
</verification>

<success_criteria>
- TreeRegistrationScreen under 300 lines (was 1054)
- Zero direct data layer imports in TreeRegistrationScreen
- 3 hooks + 3 components extracted and working
- All existing tests still pass
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-strategy/09-02-SUMMARY.md`
</output>
