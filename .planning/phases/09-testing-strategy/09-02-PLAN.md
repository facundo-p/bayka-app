---
phase: 09-testing-strategy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/screens/TreeRegistrationScreen.tsx
  - mobile/src/hooks/useTreeRegistration.ts
  - mobile/src/hooks/useSpeciesOrder.ts
  - mobile/src/hooks/useNNFlow.ts
  - mobile/src/components/SpeciesGrid.tsx
  - mobile/src/components/LastThreeTrees.tsx
  - mobile/src/components/TreeRegistrationHeader.tsx
autonomous: true
requirements:
  - TEST-REFACTOR

must_haves:
  truths:
    - "TreeRegistrationScreen.tsx is under 300 lines"
    - "All tree registration logic lives in hooks, not the screen"
    - "Screen imports NO repositories, queries, or services directly"
    - "App still works identically after refactor (no behavioral change)"
  artifacts:
    - path: "mobile/src/hooks/useTreeRegistration.ts"
      provides: "Tree CRUD logic (register, undo, finalize)"
      exports: ["useTreeRegistration"]
    - path: "mobile/src/hooks/useSpeciesOrder.ts"
      provides: "Species ordering and button grid state"
      exports: ["useSpeciesOrder"]
    - path: "mobile/src/hooks/useNNFlow.ts"
      provides: "N/N tree registration and photo capture flow"
      exports: ["useNNFlow"]
    - path: "mobile/src/screens/TreeRegistrationScreen.tsx"
      provides: "Presentation-only screen composing hooks + components"
      min_lines: 50
  key_links:
    - from: "mobile/src/screens/TreeRegistrationScreen.tsx"
      to: "mobile/src/hooks/useTreeRegistration.ts"
      via: "hook import"
      pattern: "import.*useTreeRegistration"
    - from: "mobile/src/hooks/useTreeRegistration.ts"
      to: "mobile/src/repositories/TreeRepository.ts"
      via: "repository call"
      pattern: "TreeRepository"
---

<objective>
Decompose TreeRegistrationScreen.tsx (1053 lines) into focused hooks and components per D-01/D-02. Extract all data access logic into hooks, all reusable UI into components. Screen becomes a thin orchestration layer.

Purpose: Makes tree registration logic independently testable without rendering. Eliminates CLAUDE.md rule 9 violation (direct repository/query imports in screen).
Output: Screen under 300 lines, 3 new hooks, 2-3 new components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-testing-strategy/09-CONTEXT.md

@mobile/src/screens/TreeRegistrationScreen.tsx
@mobile/src/hooks/useTrees.ts
@mobile/src/hooks/usePlantationSpecies.ts
@mobile/src/hooks/usePhotoPicker.ts
@mobile/src/repositories/TreeRepository.ts
@mobile/src/repositories/SubGroupRepository.ts
@mobile/src/repositories/UserSpeciesOrderRepository.ts
@mobile/src/services/PhotoService.ts
@mobile/src/queries/plantationDetailQueries.ts
@mobile/src/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract hooks from TreeRegistrationScreen</name>
  <files>
    mobile/src/hooks/useTreeRegistration.ts
    mobile/src/hooks/useSpeciesOrder.ts
    mobile/src/hooks/useNNFlow.ts
  </files>
  <read_first>
    mobile/src/screens/TreeRegistrationScreen.tsx
    mobile/src/hooks/useTrees.ts
    mobile/src/hooks/usePlantationSpecies.ts
    mobile/src/hooks/usePhotoPicker.ts
    mobile/src/repositories/TreeRepository.ts
    mobile/src/repositories/SubGroupRepository.ts
    mobile/src/repositories/UserSpeciesOrderRepository.ts
    mobile/src/services/PhotoService.ts
    mobile/src/queries/plantationDetailQueries.ts
  </read_first>
  <action>
Analyze TreeRegistrationScreen.tsx and identify 3 logic clusters to extract:

**Hook 1: `useTreeRegistration.ts`** — Core tree CRUD operations:
- Register tree by species tap (calls TreeRepository.createTree)
- Undo last tree (calls TreeRepository.deleteTree)
- Get current tree count and last 3 trees
- Finalize subgroup logic (calls SubGroupRepository.finalize)
- Reverse order (calls reverseTreeOrder or similar)
- Takes: subgrupoId, plantacionId, subgrupoCodigo
- Returns: { trees, treeCount, lastThree, registerTree, undoLast, finalizeSubgroup, reverseOrder, loading }
- Imports from: TreeRepository, SubGroupRepository, plantationDetailQueries (NOT from screen)

**Hook 2: `useSpeciesOrder.ts`** — Species button ordering:
- Load plantation species with user-customized order
- Reorder species buttons
- Takes: plantacionId, userId
- Returns: { orderedSpecies, reorderSpecies, loading }
- Imports from: UserSpeciesOrderRepository, PlantationSpeciesRepository

**Hook 3: `useNNFlow.ts`** — N/N registration flow:
- Register N/N tree (requires photo)
- Photo capture via usePhotoPicker
- Check if subgroup has unresolved N/N trees
- Takes: subgrupoId, plantacionId
- Returns: { registerNN, hasUnresolvedNN, nnCount, capturePhoto }
- Imports from: TreeRepository, PhotoService, usePhotoPicker

Each hook:
- Imports ONLY from repositories/queries/services/other hooks (CLAUDE.md rule 9)
- Uses existing useLiveData/useTrees pattern for reactive updates
- Exports a single named hook function
- All state management (useState, useEffect, useCallback) lives in the hook, not the screen

Preserve ALL existing behavior exactly. This is a pure extraction refactor — no new features, no changed logic.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mobile/src/hooks/useTreeRegistration.ts exists and exports useTreeRegistration
    - mobile/src/hooks/useSpeciesOrder.ts exists and exports useSpeciesOrder
    - mobile/src/hooks/useNNFlow.ts exists and exports useNNFlow
    - None of the 3 new hooks import from 'src/screens/'
    - All 3 hooks import from repositories/queries/services only
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>3 hooks extracted with all tree registration logic, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Slim TreeRegistrationScreen to presentation-only + extract components</name>
  <files>
    mobile/src/screens/TreeRegistrationScreen.tsx
    mobile/src/components/SpeciesGrid.tsx
    mobile/src/components/LastThreeTrees.tsx
    mobile/src/components/TreeRegistrationHeader.tsx
  </files>
  <read_first>
    mobile/src/screens/TreeRegistrationScreen.tsx
    mobile/src/hooks/useTreeRegistration.ts
    mobile/src/hooks/useSpeciesOrder.ts
    mobile/src/hooks/useNNFlow.ts
    mobile/src/theme.ts
  </read_first>
  <action>
1. Extract reusable UI components from TreeRegistrationScreen:

**SpeciesGrid.tsx** — Species button grid:
- Props: { species: OrderedSpecies[], onSpeciesTap: (speciesId: string) => void, onNNTap: () => void }
- Renders the grid of species buttons + N/N button
- Styles from theme.ts (per CLAUDE.md rule 8)

**LastThreeTrees.tsx** — Last 3 registered trees display:
- Props: { trees: Tree[], onUndo: () => void, canUndo: boolean }
- Renders last 3 tree entries with undo button

**TreeRegistrationHeader.tsx** — Header with count and actions:
- Props: { treeCount: number, subgroupName: string, onReverse: () => void, onFinalize: () => void, canFinalize: boolean, canReverse: boolean }
- Renders tree count, subgroup info, action buttons

2. Rewrite TreeRegistrationScreen.tsx as thin orchestration:
- Import and call useTreeRegistration, useSpeciesOrder, useNNFlow
- Import SpeciesGrid, LastThreeTrees, TreeRegistrationHeader
- NO direct imports from repositories, queries, services, or `db`
- Screen only does: route params extraction, hook composition, component rendering
- Target: under 300 lines (ideally 150-200)

3. Verify all existing behavior preserved:
- Species button tap registers tree
- N/N flow captures photo
- Undo removes last tree
- Reverse order works
- Finalize with N/N gate works

4. Run existing tests to ensure no regressions:
   ```bash
   cd mobile && npx jest --ci
   ```
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit && wc -l src/screens/TreeRegistrationScreen.tsx && npx jest --ci 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TreeRegistrationScreen.tsx is under 300 lines (verified by `wc -l`)
    - TreeRegistrationScreen.tsx does NOT contain "import.*Repository" or "import.*Service" or "import.*queries"
    - TreeRegistrationScreen.tsx imports useTreeRegistration, useSpeciesOrder, useNNFlow
    - mobile/src/components/SpeciesGrid.tsx exists
    - mobile/src/components/LastThreeTrees.tsx exists
    - mobile/src/components/TreeRegistrationHeader.tsx exists
    - `npx tsc --noEmit` passes
    - `npx jest --ci` passes with 0 failures
  </acceptance_criteria>
  <done>TreeRegistrationScreen under 300 lines, all logic in hooks, all reusable UI in components, zero CLAUDE.md violations, all tests pass</done>
</task>

</tasks>

<verification>
- `wc -l mobile/src/screens/TreeRegistrationScreen.tsx` < 300
- `grep -c "import.*Repository\|import.*Service\|import.*queries" mobile/src/screens/TreeRegistrationScreen.tsx` = 0
- `cd mobile && npx tsc --noEmit` passes
- `cd mobile && npx jest --ci` passes
</verification>

<success_criteria>
TreeRegistrationScreen decomposed from 1053 lines to under 300 lines. All business logic in testable hooks. All UI in reusable components. Zero direct data access imports in screen. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-strategy/09-02-SUMMARY.md`
</output>
