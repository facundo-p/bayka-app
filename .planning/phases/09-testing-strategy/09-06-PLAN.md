---
phase: 09-testing-strategy
plan: 06
type: execute
wave: 3
depends_on: ["09-02", "09-03", "09-04", "09-05"]
files_modified:
  - mobile/.maestro/flows/01-login-offline.yaml
  - mobile/.maestro/flows/02-register-tree.yaml
  - mobile/.maestro/flows/03-sync-subgroup.yaml
  - mobile/.maestro/helpers/login.yaml
  - mobile/src/app/(auth)/login.tsx
  - mobile/src/screens/TreeRegistrationScreen.tsx
  - mobile/src/components/SpeciesGrid.tsx
  - mobile/src/screens/PlantacionesScreen.tsx
  - mobile/src/screens/PlantationDetailScreen.tsx
  - .github/workflows/e2e.yml
autonomous: false
requirements:
  - TEST-E2E
  - TEST-CI-E2E
  - TEST-CI

must_haves:
  truths:
    - "Maestro flows exist for 3 critical user journeys"
    - "Login offline flow tests cached credential login"
    - "Register tree flow tests species tap and tree creation"
    - "Sync flow tests subgroup upload"
    - "E2E GitHub Actions workflow targets PR to main with macOS runner"
    - "Branch protection on main requires all CI checks to pass before merge (per D-14)"
  artifacts:
    - path: "mobile/.maestro/flows/01-login-offline.yaml"
      provides: "E2E flow: login with cached credentials offline"
      contains: "launchApp"
    - path: "mobile/.maestro/flows/02-register-tree.yaml"
      provides: "E2E flow: navigate to subgroup, tap species, verify tree registered"
      contains: "tapOn"
    - path: "mobile/.maestro/flows/03-sync-subgroup.yaml"
      provides: "E2E flow: finalize and sync subgroup"
      contains: "tapOn"
    - path: ".github/workflows/e2e.yml"
      provides: "GitHub Actions E2E pipeline with Maestro on macOS runner"
      contains: "macos-latest"
  key_links:
    - from: "mobile/.maestro/flows/02-register-tree.yaml"
      to: "mobile/src/components/SpeciesGrid.tsx"
      via: "testID='species-btn-{code}' on species buttons"
      pattern: "species-btn"
    - from: "mobile/.maestro/flows/*.yaml"
      to: "mobile/src/screens/*.tsx"
      via: "testID props on interactive elements"
      pattern: "testID"
---

<objective>
Create 3 Maestro E2E flows for critical user journeys (per D-06): offline login, tree registration, SubGroup sync. Add testID props to key interactive elements. Create GitHub Actions E2E workflow for PR-to-main gate (per D-13). Instruct user to enable branch protection on main (per D-14).

Purpose: E2E flows validate the full user journey from UI to database. Maestro YAML flows are maintainable and run on iOS simulator.
Output: 3 Maestro flow files, 1 helper, E2E CI workflow, testIDs on critical elements, branch protection guidance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-testing-strategy/09-CONTEXT.md
@.planning/phases/09-testing-strategy/09-RESEARCH.md
@.planning/phases/09-testing-strategy/09-02-SUMMARY.md
@.planning/phases/09-testing-strategy/09-03-SUMMARY.md

@mobile/app.json
@mobile/src/screens/TreeRegistrationScreen.tsx
@mobile/src/components/SpeciesGrid.tsx
@mobile/src/screens/PlantacionesScreen.tsx
@mobile/src/app/(auth)/login.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testID props to critical interactive elements and create Maestro flows</name>
  <files>
    mobile/src/app/(auth)/login.tsx
    mobile/src/components/SpeciesGrid.tsx
    mobile/src/screens/PlantacionesScreen.tsx
    mobile/src/screens/PlantationDetailScreen.tsx
    mobile/src/screens/TreeRegistrationScreen.tsx
    mobile/.maestro/flows/01-login-offline.yaml
    mobile/.maestro/flows/02-register-tree.yaml
    mobile/.maestro/flows/03-sync-subgroup.yaml
    mobile/.maestro/helpers/login.yaml
  </files>
  <read_first>
    mobile/app.json
    mobile/src/app/(auth)/login.tsx
    mobile/src/screens/TreeRegistrationScreen.tsx
    mobile/src/components/SpeciesGrid.tsx
    mobile/src/screens/PlantacionesScreen.tsx
    mobile/src/screens/PlantationDetailScreen.tsx
  </read_first>
  <action>
1. **Add testID props** to key interactive elements (minimal — only what Maestro needs):

In `login.tsx`:
- Email input: `testID="email-input"`
- Password input: `testID="password-input"`
- Login button: `testID="login-button"`
- Email chip buttons (if they exist): `testID="email-chip-{email}"`

In `PlantacionesScreen.tsx`:
- Plantation list: `testID="plantaciones-list"`
- Each plantation card: `testID="plantation-card-{id}"`

In `PlantationDetailScreen.tsx`:
- Subgroup list: `testID="subgroup-list"`
- Each subgroup card: `testID="subgroup-card-{id}"`
- Sync button: `testID="sync-button"`

In `SpeciesGrid.tsx` (NOT TreeRegistrationScreen — after 09-02 refactor, species buttons live in SpeciesGrid component):
- Each species button: `testID="species-btn-{code}"`
- N/N button: `testID="nn-button"`

In `TreeRegistrationScreen.tsx` (only non-species-grid elements):
- Undo button: `testID="undo-button"`
- Finalize button: `testID="finalize-button"`
- Tree count display: `testID="tree-count"`

2. **Get bundle ID** from app.json (field `expo.ios.bundleIdentifier` or `expo.android.package`). Use this in Maestro flows.

3. **Create Maestro directory structure:**
   ```
   mobile/.maestro/
   ├── flows/
   │   ├── 01-login-offline.yaml
   │   ├── 02-register-tree.yaml
   │   └── 03-sync-subgroup.yaml
   └── helpers/
       └── login.yaml
   ```

4. **Create `helpers/login.yaml`** — Reusable login subflow:
   ```yaml
   appId: {bundleId from app.json}
   ---
   - tapOn:
       id: "email-input"
   - inputText: ${EMAIL}
   - tapOn:
       id: "password-input"
   - inputText: ${PASSWORD}
   - tapOn:
       id: "login-button"
   ```

5. **Create `flows/01-login-offline.yaml`:**
   - Launch app with clearState: true
   - Login online first (to cache credentials) — use test user email/password
   - Wait for plantaciones screen
   - Kill app
   - Enable airplane mode (Maestro `toggleAirplaneMode` or note this is simulator-dependent)
   - Re-launch app
   - Login with same credentials
   - Assert plantaciones screen is visible
   - Note: offline simulation in Maestro is limited — document this as a known limitation. The flow tests the cached credential UI path.

6. **Create `flows/02-register-tree.yaml`:**
   - Launch app, login
   - Tap first plantation card
   - Tap first subgroup card (or create one if needed)
   - Tap a species button (uses testID="species-btn-{code}" from SpeciesGrid.tsx)
   - Assert tree count incremented
   - Tap undo button
   - Assert tree count decremented

7. **Create `flows/03-sync-subgroup.yaml`:**
   - Launch app, login
   - Navigate to plantation -> subgroup
   - Tap finalize button
   - Navigate back to plantation detail
   - Tap sync button
   - Assert sync completes (look for success indicator)

Each flow should have realistic wait times (`waitForAnimationToEnd` or `extendedWaitUntil`) for navigation transitions.
  </action>
  <verify>
    <automated>ls -la mobile/.maestro/flows/ mobile/.maestro/helpers/ && grep -c "species-btn" mobile/src/components/SpeciesGrid.tsx && cd mobile && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mobile/.maestro/flows/01-login-offline.yaml exists and contains "launchApp" and "email-input"
    - mobile/.maestro/flows/02-register-tree.yaml exists and contains "species-btn"
    - mobile/.maestro/flows/03-sync-subgroup.yaml exists and contains "sync-button"
    - mobile/.maestro/helpers/login.yaml exists
    - login.tsx contains testID="email-input" and testID="login-button"
    - mobile/src/components/SpeciesGrid.tsx contains testID="species-btn" (definitively in SpeciesGrid, not TreeRegistrationScreen)
    - `cd mobile && npx tsc --noEmit` passes (testID additions don't break types)
  </acceptance_criteria>
  <done>3 Maestro E2E flows created, testIDs added to critical interactive elements (species-btn in SpeciesGrid.tsx), helper subflow for login reuse</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions E2E workflow</name>
  <files>
    .github/workflows/e2e.yml
  </files>
  <read_first>
    .github/workflows/ci.yml
    mobile/app.json
    .planning/phases/09-testing-strategy/09-RESEARCH.md
  </read_first>
  <action>
Create `.github/workflows/e2e.yml` for Maestro E2E on macOS runner (per D-13):

**Trigger:** pull_request to main only (not on every push — macOS minutes are expensive).

**Single job: `e2e-ios`**
- runs-on: macos-latest
- steps:
  1. actions/checkout@v4
  2. actions/setup-node@v4 with node-version '20', cache 'npm', cache-dependency-path 'mobile/package-lock.json'
  3. `npm ci` in mobile/
  4. Install Maestro CLI: `curl -Ls "https://get.maestro.mobile.dev" | bash` and add to PATH
  5. Install IDB for iOS simulator control: `brew tap facebook/fb && brew install facebook/fb/idb-companion`
  6. Expo prebuild: `npx expo prebuild --platform ios --clean` in mobile/
  7. Build iOS simulator app:
     ```
     xcodebuild -workspace ios/mobile.xcworkspace \
       -scheme mobile \
       -configuration Release \
       -destination 'generic/platform=iOS Simulator' \
       -derivedDataPath build/
     ```
     working-directory: mobile
  8. Boot iOS simulator:
     ```
     SIMULATOR_ID=$(xcrun simctl list devices available -j | python3 -c "import sys,json; devs=json.load(sys.stdin)['devices']; iphone=[d for v in devs.values() for d in v if 'iPhone' in d['name'] and d['isAvailable']]; print(iphone[0]['udid'])")
     xcrun simctl boot $SIMULATOR_ID
     xcrun simctl install $SIMULATOR_ID mobile/build/Build/Products/Release-iphonesimulator/mobile.app
     ```
  9. Run Maestro flows: `maestro test mobile/.maestro/flows/`

Add a comment at top of the file noting macOS runner cost (10x Ubuntu minutes for private repos).

Verify YAML syntax is valid.
  </action>
  <verify>
    <automated>cat .github/workflows/e2e.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/e2e.yml'))" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/e2e.yml exists
    - e2e.yml triggers on pull_request to main only
    - e2e.yml uses runs-on: macos-latest
    - e2e.yml contains "maestro test" command
    - e2e.yml contains "expo prebuild" step
    - e2e.yml contains "xcodebuild" step
    - YAML parses without errors
  </acceptance_criteria>
  <done>GitHub Actions E2E workflow created for Maestro on macOS runner, triggering on PR to main</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Enable branch protection on main and verify E2E flows and CI pipeline</name>
  <files>N/A — human verification and GitHub settings</files>
  <action>
Human verifies the complete testing infrastructure and enables branch protection (per D-14):

**Part 1 — Branch protection (per D-14):**
Enable branch protection rules on main in GitHub repository settings:
1. Go to GitHub repo -> Settings -> Branches -> Add rule
2. Branch name pattern: `main`
3. Enable "Require status checks to pass before merging"
4. Add required checks: `lint`, `unit`, `integration`, `e2e-ios`
5. Save changes

**Part 2 — Review E2E and CI:**
1. Review Maestro flows in `mobile/.maestro/flows/` — check that element IDs match the testIDs added to components
2. Review CI workflows in `.github/workflows/` — ci.yml for branch push, e2e.yml for PR to main
3. Optionally run Maestro locally: `cd mobile && maestro test .maestro/flows/02-register-tree.yaml` (requires Maestro CLI + iOS simulator)
4. Push a branch and verify GitHub Actions triggers the CI workflow
  </action>
  <verify>Human confirms flows, CI pipeline, and branch protection are correct. Type "approved" or describe issues.</verify>
  <done>Branch protection enabled on main requiring lint, unit, integration, and e2e-ios checks. E2E flows and CI pipeline approved by user.</done>
</task>

</tasks>

<verification>
- `ls mobile/.maestro/flows/` shows 3 YAML files
- `.github/workflows/ci.yml` and `.github/workflows/e2e.yml` both exist and parse as valid YAML
- `cd mobile && npx tsc --noEmit` passes (testID additions compile)
- `cd mobile && npx jest --ci` passes (no regressions from testID additions)
- Branch protection enabled on main with required checks
</verification>

<success_criteria>
Maestro E2E foundation established: 3 flows covering critical user journeys (D-06), testIDs on key elements (species-btn in SpeciesGrid.tsx), E2E CI pipeline on macOS runner for PR gate (D-13). Branch protection on main requiring all CI checks (D-14). Complete two-tier CI: fast checks on every push, full E2E on PR to main.
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-strategy/09-06-SUMMARY.md`
</output>
