---
phase: 09-testing-strategy
plan: 05
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - mobile/tests/repositories/TreeRepository.test.ts
  - mobile/tests/repositories/PlantationSpeciesRepository.test.ts
  - mobile/tests/repositories/UserSpeciesOrderRepository.test.ts
  - mobile/tests/services/PhotoService.test.ts
  - mobile/tests/hooks/useAuth.test.ts
  - mobile/tests/hooks/useSync.test.ts
  - mobile/tests/hooks/useTreeRegistration.test.ts
autonomous: true
requirements:
  - TEST-UNIT

must_haves:
  truths:
    - "TreeRepository has unit tests for all public methods"
    - "useAuth hook has unit tests for online login, offline login, logout"
    - "useSync hook has unit tests for sync initiation and error handling"
    - "useTreeRegistration hook has unit tests for register, undo, finalize"
    - "All new unit tests pass alongside existing tests"
  artifacts:
    - path: "mobile/tests/repositories/TreeRepository.test.ts"
      provides: "Unit tests for TreeRepository CRUD methods"
      contains: "TreeRepository"
    - path: "mobile/tests/hooks/useAuth.test.ts"
      provides: "Unit tests for useAuth hook (online/offline login, logout)"
      contains: "useAuth"
    - path: "mobile/tests/hooks/useSync.test.ts"
      provides: "Unit tests for useSync hook"
      contains: "useSync"
    - path: "mobile/tests/hooks/useTreeRegistration.test.ts"
      provides: "Unit tests for the extracted useTreeRegistration hook"
      contains: "useTreeRegistration"
  key_links:
    - from: "mobile/tests/hooks/useAuth.test.ts"
      to: "mobile/tests/helpers/networkHelper.ts"
      via: "setOffline/setOnline imports for offline login tests"
      pattern: "import.*networkHelper"
    - from: "mobile/tests/hooks/useTreeRegistration.test.ts"
      to: "mobile/src/hooks/useTreeRegistration.ts"
      via: "hook under test"
      pattern: "import.*useTreeRegistration"
---

<objective>
Fill critical unit test gaps for untested repositories, services, and hooks (per D-03/D-04). Focus on data+service layer. Uses mock-based approach (D-07) for fast feedback.

Purpose: Covers the untested critical paths identified in research — TreeRepository (0 tests), key hooks (useAuth, useSync — 0 tests), and the newly extracted useTreeRegistration hook.
Output: 7 new test files, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-testing-strategy/09-CONTEXT.md
@.planning/phases/09-testing-strategy/09-02-SUMMARY.md
@.planning/phases/09-testing-strategy/09-03-SUMMARY.md

@mobile/tests/helpers/networkHelper.ts
@mobile/tests/helpers/factories.ts
@mobile/src/repositories/TreeRepository.ts
@mobile/src/repositories/PlantationSpeciesRepository.ts
@mobile/src/repositories/UserSpeciesOrderRepository.ts
@mobile/src/services/PhotoService.ts
@mobile/src/hooks/useAuth.ts
@mobile/src/hooks/useSync.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for untested repositories and PhotoService</name>
  <files>
    mobile/tests/repositories/TreeRepository.test.ts
    mobile/tests/repositories/PlantationSpeciesRepository.test.ts
    mobile/tests/repositories/UserSpeciesOrderRepository.test.ts
    mobile/tests/services/PhotoService.test.ts
  </files>
  <read_first>
    mobile/src/repositories/TreeRepository.ts
    mobile/src/repositories/PlantationSpeciesRepository.ts
    mobile/src/repositories/UserSpeciesOrderRepository.ts
    mobile/src/services/PhotoService.ts
    mobile/tests/database/subgroup.test.ts
    mobile/tests/database/tree.test.ts
    mobile/tests/setup.ts
  </read_first>
  <behavior>
**TreeRepository.test.ts:**
- Test: createTree inserts row with correct fields (species, position, subgrupoId, subId)
- Test: deleteTree removes specific tree by ID
- Test: getTreesBySubgroup returns trees ordered by position
- Test: getTreeCount returns correct count for a subgroup
- Test: getLastNTrees returns last N trees by position descending
- Test: getUnresolvedNNTrees returns only trees where especieId is null

**PlantationSpeciesRepository.test.ts:**
- Test: addSpeciesToPlantation creates link record
- Test: removeSpeciesFromPlantation deletes link record
- Test: getSpeciesForPlantation returns species list for given plantation

**UserSpeciesOrderRepository.test.ts:**
- Test: saveOrder persists species order for user+plantation
- Test: getOrder returns saved order
- Test: saveOrder overwrites previous order

**PhotoService.test.ts:**
- Test: savePhoto writes to file system (mock expo-file-system)
- Test: deletePhoto removes file (mock expo-file-system)
- Test: getPhotoUri returns correct path
  </behavior>
  <action>
Follow existing test patterns from the codebase (read subgroup.test.ts and tree.test.ts for mock patterns):

1. Use Drizzle mock chain pattern established in Phase 3:
   - Mock `../../src/database/client` with `jest.mock`
   - Intermediate methods (from, innerJoin, where) return chain object
   - Terminal methods (groupBy, orderBy, get, all) resolve to arrays
   - Re-init chain in beforeEach after jest.resetAllMocks()

2. Use plain JS object chain mocks for any Supabase calls (not jest.fn() — survives resetAllMocks per Phase 6 decision)

3. For PhotoService: mock `expo-file-system` (already mocked in tests/__mocks__/expo-file-system.js) and test the service logic

4. Use factory functions from `tests/helpers/factories.ts` where appropriate for test data

5. Each test file follows pattern:
   ```typescript
   jest.mock('../../src/database/client', () => ({ db: mockDb }));

   describe('TreeRepository', () => {
     beforeEach(() => { jest.resetAllMocks(); /* re-init mock chain */ });
     // tests...
   });
   ```

Run: `cd mobile && npx jest --testPathPattern="repositories|services/PhotoService" --ci`
  </action>
  <verify>
    <automated>cd mobile && npx jest --testPathPattern="(repositories/TreeRepository|repositories/PlantationSpecies|repositories/UserSpeciesOrder|services/PhotoService)" --ci 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mobile/tests/repositories/TreeRepository.test.ts exists with at least 5 test cases
    - mobile/tests/repositories/PlantationSpeciesRepository.test.ts exists with at least 3 test cases
    - mobile/tests/repositories/UserSpeciesOrderRepository.test.ts exists with at least 3 test cases
    - mobile/tests/services/PhotoService.test.ts exists with at least 3 test cases
    - All use jest.mock for database/client (mock-based, not integration)
    - All tests pass
  </acceptance_criteria>
  <done>4 new test files covering 3 untested repositories and PhotoService, 14+ test cases, all passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for useAuth, useSync, and useTreeRegistration hooks</name>
  <files>
    mobile/tests/hooks/useAuth.test.ts
    mobile/tests/hooks/useSync.test.ts
    mobile/tests/hooks/useTreeRegistration.test.ts
  </files>
  <read_first>
    mobile/src/hooks/useAuth.ts
    mobile/src/hooks/useSync.ts
    mobile/src/hooks/useTreeRegistration.ts
    mobile/tests/helpers/networkHelper.ts
    mobile/tests/hooks/useNetStatus.test.ts
    mobile/tests/hooks/useProfileData.test.ts
    mobile/tests/setup.ts
  </read_first>
  <behavior>
**useAuth.test.ts:**
- Test: signIn with valid credentials online calls supabase.auth.signInWithPassword and caches credentials
- Test: signIn when offline falls back to OfflineAuthService.verifyCredential
- Test: signIn when offline with no cached credentials returns error
- Test: signOut clears session, cached credentials, and SecureStore keys
- Test: restoreSession checks connectivity before calling supabase.auth.setSession
- Test: role is read from SecureStore when available

**useSync.test.ts:**
- Test: sync calls SyncService methods in correct order (pull then push)
- Test: sync accumulates errors from multiple subgroup uploads
- Test: sync calls notifyDataChanged in finally block (even on error)
- Test: sync with no finalizada subgroups completes without RPC calls

**useTreeRegistration.test.ts:**
- Test: registerTree calls TreeRepository.createTree with correct params
- Test: undoLast calls TreeRepository.deleteTree for the most recent tree
- Test: trees state updates after registration (via useLiveData or similar)
- Test: finalizeSubgroup checks for unresolved N/N before proceeding
  </behavior>
  <action>
1. Read the existing hook test patterns (useNetStatus.test.ts, useProfileData.test.ts) to understand how hooks are tested in this project. Determine if they use @testing-library/react-native's `renderHook` or a custom approach.

2. For **useAuth.test.ts**:
   - Mock supabase client (signInWithPassword, setSession, signOut)
   - Mock SecureStore (getItemAsync, setItemAsync, deleteItemAsync)
   - Mock OfflineAuthService (verifyCredential, cacheCredential, clearCredential)
   - Use networkHelper's setOffline/setOnline (per D-08, D-09)
   - Test the signIn flow branching: online path vs offline fallback
   - Test signOut clears all stored keys

3. For **useSync.test.ts**:
   - Mock SyncService (syncPlantation, pullFromServer, etc.)
   - Mock notifyDataChanged from liveQuery
   - Test orchestration order and error accumulation

4. For **useTreeRegistration.test.ts**:
   - Mock TreeRepository, SubGroupRepository
   - Test the hook's public API (registerTree, undoLast, finalizeSubgroup)
   - This tests the NEW hook extracted in Plan 02

5. All hook tests use `jest.resetAllMocks()` in beforeEach (project standard, except offlineAuth which uses clearAllMocks)

Run: `cd mobile && npx jest --testPathPattern="hooks/(useAuth|useSync|useTreeRegistration)" --ci`
  </action>
  <verify>
    <automated>cd mobile && npx jest --testPathPattern="hooks/(useAuth|useSync|useTreeRegistration)" --ci 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mobile/tests/hooks/useAuth.test.ts exists with at least 5 test cases
    - mobile/tests/hooks/useSync.test.ts exists with at least 3 test cases
    - mobile/tests/hooks/useTreeRegistration.test.ts exists with at least 3 test cases
    - useAuth tests use networkHelper (setOffline/setOnline) not inline NetInfo mocks
    - All tests pass
    - `cd mobile && npx jest --ci` (full suite) passes with 0 failures
  </acceptance_criteria>
  <done>3 new hook test files covering useAuth (online+offline), useSync (orchestration), useTreeRegistration (CRUD). All passing. Full test suite green.</done>
</task>

</tasks>

<verification>
- `cd mobile && npx jest --ci` — full suite passes (unit + existing tests)
- `cd mobile && npx jest --config jest.integration.config.js --ci` — integration tests still pass
- Total new test files: 7
- Total new test cases: 25+
</verification>

<success_criteria>
All critical unit test gaps filled: TreeRepository, PlantationSpeciesRepository, UserSpeciesOrderRepository, PhotoService, useAuth, useSync, useTreeRegistration all have dedicated test files. Offline tests use centralized networkHelper. Full test suite (unit + integration) green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-strategy/09-05-SUMMARY.md`
</output>
