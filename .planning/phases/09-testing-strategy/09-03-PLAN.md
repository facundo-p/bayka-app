---
phase: 09-testing-strategy
plan: 03
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - mobile/src/screens/AdminScreen.tsx
  - mobile/src/screens/PlantationDetailScreen.tsx
  - mobile/src/screens/ConfigureSpeciesScreen.tsx
  - mobile/src/screens/CatalogScreen.tsx
  - mobile/src/screens/NNResolutionScreen.tsx
  - mobile/src/screens/AssignTechniciansScreen.tsx
  - mobile/src/screens/NuevoSubgrupoScreen.tsx
  - mobile/src/screens/PlantacionesScreen.tsx
  - mobile/src/hooks/usePlantationAdmin.ts
  - mobile/src/hooks/usePlantationDetail.ts
  - mobile/src/hooks/useSpeciesConfig.ts
  - mobile/src/hooks/useCatalog.ts
  - mobile/src/hooks/useNNResolution.ts
  - mobile/src/hooks/useAssignTechnicians.ts
autonomous: true
requirements:
  - TEST-REFACTOR

must_haves:
  truths:
    - "AdminScreen.tsx is under 350 lines"
    - "PlantationDetailScreen.tsx is under 350 lines"
    - "Zero screens import directly from repositories, queries, services, or database/client"
    - "ConfigureSpeciesScreen no longer imports db from database/client"
    - "All existing tests pass after refactor"
  artifacts:
    - path: "mobile/src/hooks/usePlantationAdmin.ts"
      provides: "Admin plantation CRUD, finalization, ID gen, export"
      min_lines: 80
    - path: "mobile/src/hooks/usePlantationDetail.ts"
      provides: "Plantation detail: subgroup list, stats, estado"
      min_lines: 50
    - path: "mobile/src/hooks/useSpeciesConfig.ts"
      provides: "Species config CRUD with offline save support"
      min_lines: 40
    - path: "mobile/src/hooks/useCatalog.ts"
      provides: "Server catalog browsing, download, delete"
      min_lines: 50
    - path: "mobile/src/hooks/useNNResolution.ts"
      provides: "N/N tree list, resolve, photo display"
      min_lines: 30
    - path: "mobile/src/hooks/useAssignTechnicians.ts"
      provides: "Technician list, assignment, unsynced count"
      min_lines: 40
  key_links:
    - from: "mobile/src/screens/AdminScreen.tsx"
      to: "mobile/src/hooks/usePlantationAdmin.ts"
      via: "hook import"
      pattern: "import.*usePlantationAdmin"
    - from: "mobile/src/screens/ConfigureSpeciesScreen.tsx"
      to: "mobile/src/hooks/useSpeciesConfig.ts"
      via: "hook import — replaces direct db import"
      pattern: "import.*useSpeciesConfig"
---

<objective>
Refactor AdminScreen (1001 lines), PlantationDetailScreen (606 lines), and fix CLAUDE.md rule 9 violations in 6 additional screens by extracting data logic into hooks.

Purpose: Complete the refactor pass (D-01/D-02) so all screens are testable, under size limits, and compliant with CLAUDE.md rule 9 (no direct data imports in screens).
Output: 6 new hooks, all 9 violating screens cleaned up, AdminScreen under 350 lines, PlantationDetailScreen under 350 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-strategy/09-RESEARCH.md
@.planning/phases/09-testing-strategy/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract hooks for AdminScreen and PlantationDetailScreen</name>
  <files>
    mobile/src/hooks/usePlantationAdmin.ts,
    mobile/src/hooks/usePlantationDetail.ts,
    mobile/src/screens/AdminScreen.tsx,
    mobile/src/screens/PlantationDetailScreen.tsx
  </files>
  <read_first>
    mobile/src/screens/AdminScreen.tsx,
    mobile/src/screens/PlantationDetailScreen.tsx,
    mobile/src/repositories/PlantationRepository.ts,
    mobile/src/queries/dashboardQueries.ts,
    mobile/src/queries/adminQueries.ts,
    mobile/src/services/ExportService.ts,
    mobile/src/queries/plantationDetailQueries.ts,
    mobile/src/repositories/SubGroupRepository.ts,
    mobile/src/hooks/useProfileData.ts,
    mobile/src/hooks/useSync.ts,
    mobile/src/theme.ts
  </read_first>
  <action>
**Hook 1: usePlantationAdmin.ts**

Extract ALL data logic from AdminScreen.tsx:

- Plantation CRUD: `createPlantation`, `createPlantationLocally`, `updatePlantation`, `discardPlantationEdit`
- Finalization: `finalizePlantation`, `checkFinalizationGate`
- ID generation: `generateIds`, `getMaxGlobalId`, `hasIdsGenerated`
- Export: `exportToCSV`, `exportToExcel`
- Dashboard data: `getPlantationsForRole`
- Import from: `PlantationRepository`, `dashboardQueries`, `adminQueries`, `ExportService`
- Accept params: `{ userId, organizacionId, isAdmin }`
- Return all functions + reactive plantation list state

AdminScreen.tsx should then only import: `usePlantationAdmin`, `useSync`, `useProfileData`, `useNetStatus`, `useConfirm`, theme, and React/RN/router modules. Target: under 350 lines.

**Hook 2: usePlantationDetail.ts**

Extract ALL data logic from PlantationDetailScreen.tsx:

- Subgroup list with estado
- Plantation estado check: `getPlantationEstado`
- Subgroup operations passed through: type imports only
- Import from: `SubGroupRepository` (type only), `plantationDetailQueries`, `adminQueries`
- Accept params: `{ plantacionId }`
- Return: `{ subgroups, plantationEstado, isLoading }`

PlantationDetailScreen.tsx should then only import: `usePlantationDetail`, `useSubGroups`, theme, React/RN/router. Target: under 350 lines.

After extracting hooks, rewrite both screens as thin composition layers. Preserve ALL existing behavior:
- AdminScreen: plantation list, create/edit modals, finalization flow, ID generation, export, offline create, pending sync badge
- PlantationDetailScreen: subgroup list with estado chips, navigation to tree registration, subgroup creation
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Trabajos/BaykaApp/bayka-app-redesign && wc -l mobile/src/screens/AdminScreen.tsx mobile/src/screens/PlantationDetailScreen.tsx && npx --prefix mobile tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l mobile/src/screens/AdminScreen.tsx` shows less than 350
    - `wc -l mobile/src/screens/PlantationDetailScreen.tsx` shows less than 350
    - `grep -c "import.*from.*repositories\|import.*from.*queries\|import.*from.*services" mobile/src/screens/AdminScreen.tsx` shows 0
    - `grep -c "import.*from.*repositories\|import.*from.*queries\|import.*from.*services" mobile/src/screens/PlantationDetailScreen.tsx` shows 0
    - `mobile/src/hooks/usePlantationAdmin.ts` exists and contains `import.*PlantationRepository`
    - `mobile/src/hooks/usePlantationDetail.ts` exists
    - `npx tsc --noEmit` produces zero errors
  </acceptance_criteria>
  <done>AdminScreen under 350 lines, PlantationDetailScreen under 350 lines. Both screens have zero direct data imports. Hooks extracted and working.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CLAUDE.md rule 9 violations in 6 remaining screens</name>
  <files>
    mobile/src/hooks/useSpeciesConfig.ts,
    mobile/src/hooks/useCatalog.ts,
    mobile/src/hooks/useNNResolution.ts,
    mobile/src/hooks/useAssignTechnicians.ts,
    mobile/src/screens/ConfigureSpeciesScreen.tsx,
    mobile/src/screens/CatalogScreen.tsx,
    mobile/src/screens/NNResolutionScreen.tsx,
    mobile/src/screens/AssignTechniciansScreen.tsx,
    mobile/src/screens/NuevoSubgrupoScreen.tsx,
    mobile/src/screens/PlantacionesScreen.tsx
  </files>
  <read_first>
    mobile/src/screens/ConfigureSpeciesScreen.tsx,
    mobile/src/screens/CatalogScreen.tsx,
    mobile/src/screens/NNResolutionScreen.tsx,
    mobile/src/screens/AssignTechniciansScreen.tsx,
    mobile/src/screens/NuevoSubgrupoScreen.tsx,
    mobile/src/screens/PlantacionesScreen.tsx,
    mobile/src/queries/catalogQueries.ts,
    mobile/src/queries/adminQueries.ts,
    mobile/src/queries/freshnessQueries.ts,
    mobile/src/repositories/PlantationRepository.ts,
    mobile/src/repositories/TreeRepository.ts,
    mobile/src/repositories/SubGroupRepository.ts,
    mobile/src/services/SyncService.ts,
    mobile/src/hooks/useSync.ts,
    mobile/src/hooks/usePendingSyncCount.ts
  </read_first>
  <action>
For each screen, extract data logic to a hook. The screen must have ZERO imports from `repositories/`, `queries/`, `services/`, or `database/`.

**ConfigureSpeciesScreen.tsx** (HIGHEST PRIORITY — imports `db` directly on line 28):
- Create `useSpeciesConfig.ts` hook
- Move: `getPlantationSpeciesConfig`, `hasTreesForSpecies`, `saveSpeciesConfig`, `saveSpeciesConfigLocally`, and the direct `db` query
- The hook handles all Drizzle queries internally
- Screen imports only the hook

**CatalogScreen.tsx**:
- Create `useCatalog.ts` hook
- Move: `getServerCatalog`, `getLocalPlantationIds`, `getUnsyncedSubgroupSummary`, `deletePlantationLocally`, `batchDownload`
- Hook manages catalog state, download progress, delete confirmation
- Screen imports only the hook

**NNResolutionScreen.tsx**:
- Create `useNNResolution.ts` hook
- Move: `resolveNNTree`, `getNNTreesForPlantation`
- Hook manages NN tree list state and resolution
- Screen imports only the hook

**AssignTechniciansScreen.tsx**:
- Create `useAssignTechnicians.ts` hook
- Move: `getAllTechnicians`, `getAssignedTechnicians`, `getTechnicianUnsyncedSubgroupCount`, `assignTechnicians`
- Hook manages technician list and assignment state
- Screen imports only the hook

**NuevoSubgrupoScreen.tsx** (82 lines — small):
- Move `createSubGroup`, `getLastSubGroupName` into the existing `useSubGroups` hook OR create a minimal `useNewSubgroup` hook
- Screen imports only the hook

**PlantacionesScreen.tsx**:
- Already has `useSync` and `usePendingSyncCount` hooks
- Move remaining direct imports: `checkFreshness` from `freshnessQueries`, `pullFromServer`/`uploadPendingEdits` from `SyncService`, `getUnsyncedSubgroupSummary` from `catalogQueries`, `deletePlantationLocally` from `PlantationRepository`
- Either expand `useSync` hook or create a thin wrapper hook
- Screen must have zero direct data imports

After all extractions, verify ALL screens in `src/screens/` have zero direct repository/query/service/database imports. Only `PerfilScreen.tsx` (already clean) and the refactored screens should remain.
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Trabajos/BaykaApp/bayka-app-redesign && grep -rl "import.*from.*repositories\|import.*from.*queries\|import.*from.*services\|import.*from.*database/client" mobile/src/screens/ 2>/dev/null | wc -l && npx --prefix mobile tsc --noEmit 2>&1 | tail -5 && cd mobile && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "import.*from.*repositories\|import.*from.*queries\|import.*from.*services\|import.*from.*database/client" mobile/src/screens/` returns 0 files
    - `mobile/src/hooks/useSpeciesConfig.ts` exists
    - `mobile/src/hooks/useCatalog.ts` exists
    - `mobile/src/hooks/useNNResolution.ts` exists
    - `mobile/src/hooks/useAssignTechnicians.ts` exists
    - `grep "import.*db.*from.*database" mobile/src/screens/ConfigureSpeciesScreen.tsx` returns nothing (the critical §9 violation is gone)
    - `npx tsc --noEmit` produces zero errors
    - `npx jest --no-coverage` all tests pass
  </acceptance_criteria>
  <done>All 9 screens with CLAUDE.md rule 9 violations are fixed. Zero screens import directly from repositories/queries/services/database. 6 new hooks created. TypeScript compiles cleanly. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `grep -rl "import.*from.*repositories\|import.*from.*queries\|import.*from.*services\|import.*from.*database" mobile/src/screens/` — returns 0 files (allowing type-only imports with `import type`)
- `wc -l mobile/src/screens/AdminScreen.tsx` < 350
- `wc -l mobile/src/screens/PlantationDetailScreen.tsx` < 350
- `cd mobile && npx tsc --noEmit` — zero errors
- `cd mobile && npx jest --no-coverage` — all tests pass
</verification>

<success_criteria>
- AdminScreen under 350 lines (was 1001)
- PlantationDetailScreen under 350 lines (was 606)
- All 9 violating screens fixed — zero direct data layer imports in any screen
- ConfigureSpeciesScreen no longer imports `db`
- 6 new hooks extracted and working
- All existing tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-strategy/09-03-SUMMARY.md`
</output>
