---
phase: 09-testing-strategy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/screens/AdminScreen.tsx
  - mobile/src/screens/PlantationDetailScreen.tsx
  - mobile/src/screens/ConfigureSpeciesScreen.tsx
  - mobile/src/screens/CatalogScreen.tsx
  - mobile/src/screens/NNResolutionScreen.tsx
  - mobile/src/screens/AssignTechniciansScreen.tsx
  - mobile/src/hooks/usePlantationAdmin.ts
  - mobile/src/hooks/usePlantationDetail.ts
  - mobile/src/hooks/useSpeciesConfig.ts
  - mobile/src/hooks/useCatalog.ts
  - mobile/src/hooks/useNNResolution.ts
  - mobile/src/hooks/useAssignTechnicians.ts
  - mobile/src/components/AdminPlantationModals.tsx
autonomous: true
requirements:
  - TEST-REFACTOR

must_haves:
  truths:
    - "AdminScreen.tsx is under 350 lines"
    - "PlantationDetailScreen.tsx is under 300 lines"
    - "No screen file imports directly from repositories, queries, services, or db"
    - "All 6 screens with CLAUDE.md violations are fixed"
    - "App behavior unchanged after refactor"
  artifacts:
    - path: "mobile/src/hooks/usePlantationAdmin.ts"
      provides: "Admin plantation CRUD, finalization, ID generation, export logic"
      exports: ["usePlantationAdmin"]
    - path: "mobile/src/hooks/usePlantationDetail.ts"
      provides: "Subgroup list, plantation stats, admin actions for detail screen"
      exports: ["usePlantationDetail"]
    - path: "mobile/src/hooks/useSpeciesConfig.ts"
      provides: "Species configuration logic (add/remove/reorder)"
      exports: ["useSpeciesConfig"]
    - path: "mobile/src/hooks/useCatalog.ts"
      provides: "Server catalog browsing and download logic"
      exports: ["useCatalog"]
    - path: "mobile/src/hooks/useNNResolution.ts"
      provides: "N/N tree resolution (photo display, species selection)"
      exports: ["useNNResolution"]
    - path: "mobile/src/hooks/useAssignTechnicians.ts"
      provides: "Technician assignment logic"
      exports: ["useAssignTechnicians"]
  key_links:
    - from: "mobile/src/screens/AdminScreen.tsx"
      to: "mobile/src/hooks/usePlantationAdmin.ts"
      via: "hook import"
      pattern: "import.*usePlantationAdmin"
    - from: "mobile/src/screens/ConfigureSpeciesScreen.tsx"
      to: "mobile/src/hooks/useSpeciesConfig.ts"
      via: "hook import replacing direct db import"
      pattern: "import.*useSpeciesConfig"
---

<objective>
Decompose AdminScreen.tsx (913 lines) and PlantationDetailScreen.tsx (605 lines) into hooks and components. Fix CLAUDE.md rule 9 violations in ConfigureSpeciesScreen (direct db import!), CatalogScreen, NNResolutionScreen, and AssignTechniciansScreen by extracting logic to hooks.

Purpose: Makes all admin and detail logic independently testable. Eliminates all remaining CLAUDE.md rule 9 violations across the codebase.
Output: 2 large screens under size limits, 6 new hooks, all screens violation-free.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-testing-strategy/09-CONTEXT.md

@mobile/src/screens/AdminScreen.tsx
@mobile/src/screens/PlantationDetailScreen.tsx
@mobile/src/screens/ConfigureSpeciesScreen.tsx
@mobile/src/screens/CatalogScreen.tsx
@mobile/src/screens/NNResolutionScreen.tsx
@mobile/src/screens/AssignTechniciansScreen.tsx
@mobile/src/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract hooks for AdminScreen, PlantationDetailScreen, and ConfigureSpeciesScreen</name>
  <files>
    mobile/src/hooks/usePlantationAdmin.ts
    mobile/src/hooks/usePlantationDetail.ts
    mobile/src/hooks/useSpeciesConfig.ts
    mobile/src/screens/AdminScreen.tsx
    mobile/src/screens/PlantationDetailScreen.tsx
    mobile/src/screens/ConfigureSpeciesScreen.tsx
    mobile/src/components/AdminPlantationModals.tsx
  </files>
  <read_first>
    mobile/src/screens/AdminScreen.tsx
    mobile/src/screens/PlantationDetailScreen.tsx
    mobile/src/screens/ConfigureSpeciesScreen.tsx
    mobile/src/repositories/PlantationRepository.ts
    mobile/src/queries/adminQueries.ts
    mobile/src/queries/dashboardQueries.ts
    mobile/src/queries/exportQueries.ts
    mobile/src/services/ExportService.ts
    mobile/src/repositories/SubGroupRepository.ts
    mobile/src/queries/plantationDetailQueries.ts
    mobile/src/repositories/UserSpeciesOrderRepository.ts
    mobile/src/repositories/PlantationSpeciesRepository.ts
    mobile/src/database/client.ts
    mobile/src/theme.ts
  </read_first>
  <action>
**Priority 1 — ConfigureSpeciesScreen (CRITICAL: imports `db` directly):**
Create `useSpeciesConfig.ts`:
- Move ALL data logic from ConfigureSpeciesScreen into this hook
- Takes: plantacionId
- Returns: { species, availableSpecies, addSpecies, removeSpecies, reorderSpecies, loading }
- Imports: PlantationSpeciesRepository, adminQueries, UserSpeciesOrderRepository (NOT `db`)
- ConfigureSpeciesScreen becomes presentation-only, imports useSpeciesConfig

**Priority 2 — AdminScreen (913 lines):**
Create `usePlantationAdmin.ts`:
- Move ALL plantation management logic: create, finalize, generate IDs, export
- Takes: organizacionId
- Returns: { plantations, createPlantation, finalizePlantation, generateIds, exportData, loading, selectedPlantation, ... }
- Imports: PlantationRepository, adminQueries, dashboardQueries, ExportService

Extract `AdminPlantationModals.tsx`:
- Create plantation modal, seed dialog, confirm modal, export modal
- Props-driven, no data access logic

Slim AdminScreen.tsx to under 350 lines — route params, hook calls, component composition only.

**Priority 3 — PlantationDetailScreen (605 lines):**
Create `usePlantationDetail.ts`:
- Move subgroup list logic, plantation stats, admin action handlers
- Takes: plantacionId
- Returns: { plantation, subgroups, stats, isFinalizada, adminActions, loading }
- Imports: SubGroupRepository, plantationDetailQueries, adminQueries

Slim PlantationDetailScreen.tsx to under 300 lines.

**Rules for all extractions:**
- Screens must NOT import from repositories/, queries/, services/, or database/
- Screens only import hooks and components
- All useState, useEffect, useCallback related to data move to hooks
- Styles stay in screens or move to components (from theme.ts per CLAUDE.md rule 8)
- Preserve exact same behavior — pure refactor, no feature changes
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit && wc -l src/screens/AdminScreen.tsx src/screens/PlantationDetailScreen.tsx src/screens/ConfigureSpeciesScreen.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - mobile/src/hooks/usePlantationAdmin.ts exists and exports usePlantationAdmin
    - mobile/src/hooks/usePlantationDetail.ts exists and exports usePlantationDetail
    - mobile/src/hooks/useSpeciesConfig.ts exists and exports useSpeciesConfig
    - ConfigureSpeciesScreen.tsx does NOT contain "import.*from.*database/client" or "import.*db"
    - AdminScreen.tsx is under 350 lines
    - PlantationDetailScreen.tsx is under 300 lines
    - None of the 3 screens import directly from repositories/queries/services
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>3 screens refactored with logic in hooks, ConfigureSpeciesScreen no longer imports db, AdminScreen under 350 lines, PlantationDetailScreen under 300 lines</done>
</task>

<task type="auto">
  <name>Task 2: Extract hooks for CatalogScreen, NNResolutionScreen, AssignTechniciansScreen and verify full suite</name>
  <files>
    mobile/src/hooks/useCatalog.ts
    mobile/src/hooks/useNNResolution.ts
    mobile/src/hooks/useAssignTechnicians.ts
    mobile/src/screens/CatalogScreen.tsx
    mobile/src/screens/NNResolutionScreen.tsx
    mobile/src/screens/AssignTechniciansScreen.tsx
  </files>
  <read_first>
    mobile/src/screens/CatalogScreen.tsx
    mobile/src/screens/NNResolutionScreen.tsx
    mobile/src/screens/AssignTechniciansScreen.tsx
    mobile/src/queries/catalogQueries.ts
    mobile/src/repositories/PlantationRepository.ts
    mobile/src/services/SyncService.ts
    mobile/src/repositories/TreeRepository.ts
    mobile/src/queries/plantationDetailQueries.ts
    mobile/src/queries/adminQueries.ts
  </read_first>
  <action>
**CatalogScreen** — Create `useCatalog.ts`:
- Move catalog browsing, selection, batch download logic
- Takes: userId, role, organizacionId
- Returns: { serverPlantations, localIds, selectedIds, toggleSelection, downloadSelected, downloading, progress }
- Imports: catalogQueries, PlantationRepository, SyncService
- CatalogScreen becomes presentation-only

**NNResolutionScreen** — Create `useNNResolution.ts`:
- Move N/N tree loading, species selection, resolution commit logic
- Takes: subgrupoId, treeId (from route params)
- Returns: { nnTree, photo, availableSpecies, resolveWithSpecies, loading }
- Imports: TreeRepository, plantationDetailQueries

**AssignTechniciansScreen** — Create `useAssignTechnicians.ts`:
- Move technician list loading, assignment toggle logic
- Takes: plantacionId
- Returns: { technicians, assignedIds, toggleAssignment, saving }
- Imports: adminQueries, PlantationRepository

After all extractions, run the full verification:
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npx jest --ci` — all tests pass
3. Verify NO screen file imports from repositories/, queries/, services/, or database/:
   ```bash
   grep -rn "from.*repositories/\|from.*queries/\|from.*services/\|from.*database/" src/screens/ | grep -v "\.test\."
   ```
   Expected: 0 matches (or only NuevoSubgrupoScreen if it's small enough to be acceptable — check and fix if needed)

Also check NuevoSubgrupoScreen.tsx (79 lines) and PlantacionesScreen.tsx (252 lines) — if they have direct imports, extract those too. These are small screens but the rule applies universally.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit && npx jest --ci 2>&1 | tail -10 && echo "---VIOLATIONS---" && grep -rn "from.*repositories/\|from.*queries/\|from.*services/\|from.*database/" src/screens/ 2>/dev/null | grep -v ".test." | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - mobile/src/hooks/useCatalog.ts exists and exports useCatalog
    - mobile/src/hooks/useNNResolution.ts exists and exports useNNResolution
    - mobile/src/hooks/useAssignTechnicians.ts exists and exports useAssignTechnicians
    - CatalogScreen.tsx does NOT import from repositories/queries/services
    - NNResolutionScreen.tsx does NOT import from repositories/queries/services
    - AssignTechniciansScreen.tsx does NOT import from repositories/queries/services
    - `grep -rn "from.*repositories/\|from.*queries/\|from.*services/\|from.*database/" src/screens/` returns 0 matches
    - `npx tsc --noEmit` passes
    - `npx jest --ci` passes with 0 failures
  </acceptance_criteria>
  <done>All screen files are CLAUDE.md rule 9 compliant — zero direct data imports. All logic in hooks. Full test suite green. Zero TypeScript errors.</done>
</task>

</tasks>

<verification>
- `grep -rn "from.*repositories/\|from.*queries/\|from.*services/\|from.*database/" mobile/src/screens/` = 0 matches
- `wc -l mobile/src/screens/AdminScreen.tsx` < 350
- `wc -l mobile/src/screens/PlantationDetailScreen.tsx` < 300
- `cd mobile && npx tsc --noEmit` passes
- `cd mobile && npx jest --ci` passes
</verification>

<success_criteria>
All 6 screens with CLAUDE.md violations are fixed. AdminScreen under 350 lines, PlantationDetailScreen under 300 lines. Every screen only imports hooks and components for data access. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-strategy/09-03-SUMMARY.md`
</output>
