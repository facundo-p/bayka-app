---
phase: 09-testing-strategy
plan: "07"
type: execute
wave: 1
depends_on: ["09-02", "09-06"]
files_modified:
  - mobile/src/screens/TreeRegistrationScreen.tsx
autonomous: true
requirements: [TEST-REFACTOR]
gap_closure: true

must_haves:
  truths:
    - "TreeRegistrationScreen imports useTreeRegistration, useSpeciesOrder, useNNFlow, usePhotoCapture — zero imports from repositories/, queries/, services/"
    - "TreeRegistrationScreen is under 300 lines"
    - "All three testIDs (tree-count, undo-button, finalize-button) are present for Maestro E2E flows"
  artifacts:
    - path: "mobile/src/screens/TreeRegistrationScreen.tsx"
      provides: "Thin composition screen using extracted hooks and components"
      max_lines: 300
  key_links:
    - from: "mobile/src/screens/TreeRegistrationScreen.tsx"
      to: "mobile/src/hooks/useTreeRegistration.ts"
      via: "import { useTreeRegistration }"
      pattern: "import.*useTreeRegistration.*from.*hooks/useTreeRegistration"
    - from: "mobile/src/screens/TreeRegistrationScreen.tsx"
      to: "mobile/src/hooks/useNNFlow.ts"
      via: "import { useNNFlow }"
      pattern: "import.*useNNFlow.*from.*hooks/useNNFlow"
    - from: "mobile/src/screens/TreeRegistrationScreen.tsx"
      to: "mobile/src/hooks/useSpeciesOrder.ts"
      via: "import { useSpeciesOrder }"
      pattern: "import.*useSpeciesOrder.*from.*hooks/useSpeciesOrder"
---

<objective>
Restore the TreeRegistrationScreen refactor that was overwritten by commit 2436f72 (testIDs commit). The refactored version existed at commit a126d09 (286 lines, zero repository/service imports) but the Maestro testIDs worktree agent overwrote it with the original 1054-line version. This plan restores the refactored screen AND adds the three testIDs needed by Maestro E2E flows.

Purpose: Close Blocker 1 from VERIFICATION.md — TreeRegistrationScreen must comply with CLAUDE.md rule 9 and be under 300 lines.
Output: TreeRegistrationScreen.tsx refactored to use extracted hooks/components, under 300 lines, with testIDs preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-strategy/09-02-SUMMARY.md
@.planning/phases/09-testing-strategy/09-06-SUMMARY.md

Root cause: Commit 2436f72 (feat(09-06): add testIDs) was executed in a worktree that had the pre-refactor version of TreeRegistrationScreen. When merged, it overwrote the refactored version from a126d09. The hooks and components extracted in 09-02 all exist and are functional — only the screen file itself needs to be restored.

The refactored version exists in git at commit a126d09. Use `git show a126d09:mobile/src/screens/TreeRegistrationScreen.tsx` as the base, then add the three testIDs from the current version.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore refactored TreeRegistrationScreen with testIDs</name>
  <files>mobile/src/screens/TreeRegistrationScreen.tsx</files>
  <read_first>
    mobile/src/screens/TreeRegistrationScreen.tsx
    mobile/src/hooks/useTreeRegistration.ts
    mobile/src/hooks/useNNFlow.ts
    mobile/src/hooks/useSpeciesOrder.ts
    mobile/src/hooks/usePhotoCapture.ts
    mobile/src/components/LastThreeTrees.tsx
    mobile/src/components/TreeRegistrationHeader.tsx
    mobile/src/components/PhotoViewerModal.tsx
    mobile/src/components/TreeListModal.tsx
    mobile/src/components/TreeConfigModal.tsx
    mobile/src/components/SpeciesReorderModal.tsx
    mobile/src/components/ReadOnlyTreeView.tsx
  </read_first>
  <action>
    Restore the refactored TreeRegistrationScreen from git commit a126d09 and add testIDs.

    Step 1: Run `git show a126d09:mobile/src/screens/TreeRegistrationScreen.tsx` to get the refactored 286-line version. This is the base.

    Step 2: Write that content to mobile/src/screens/TreeRegistrationScreen.tsx.

    Step 3: Add the three testIDs needed by Maestro E2E flows (from .maestro/flows/02-register-tree.yaml):
    - `testID="tree-count"` on the tree count Text element (inside TreeRegistrationHeader or wherever the count is rendered)
    - `testID="undo-button"` on the undo Pressable in LastThreeTrees
    - `testID="finalize-button"` on the finalize button

    IMPORTANT: The testIDs may need to go into the extracted components (LastThreeTrees.tsx, TreeRegistrationHeader.tsx) rather than the screen itself, since those components now render those elements. Check where the count, undo, and finalize button are rendered:
    - TreeRegistrationHeader renders the tree count — add `testID="tree-count"` there
    - LastThreeTrees renders the undo button — check if it already has testID="undo-button", add if missing
    - The finalize button is in the screen itself (bottom action bar) — add `testID="finalize-button"` there

    Step 4: Verify the screen has ZERO imports from repositories/, queries/, services/ directories. The only data-layer imports should be from hooks/.

    Step 5: Verify the screen is under 300 lines.

    Files that may also need testID additions:
    - mobile/src/components/TreeRegistrationHeader.tsx — add testID="tree-count" on the count Text
    - mobile/src/components/LastThreeTrees.tsx — add testID="undo-button" on the undo Pressable if not present
  </action>
  <verify>
    <automated>cd mobile && wc -l src/screens/TreeRegistrationScreen.tsx && grep -c "import.*from.*repositories/\|import.*from.*queries/\|import.*from.*services/" src/screens/TreeRegistrationScreen.tsx; grep "testID" src/screens/TreeRegistrationScreen.tsx src/components/TreeRegistrationHeader.tsx src/components/LastThreeTrees.tsx; npx jest --ci --no-coverage --testPathPattern="useTreeRegistration" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l mobile/src/screens/TreeRegistrationScreen.tsx` outputs a number less than 300
    - `grep -c "import.*from.*repositories/\|import.*from.*queries/\|import.*from.*services/" mobile/src/screens/TreeRegistrationScreen.tsx` outputs 0
    - `grep "useTreeRegistration" mobile/src/screens/TreeRegistrationScreen.tsx` returns a match
    - `grep "useNNFlow" mobile/src/screens/TreeRegistrationScreen.tsx` returns a match
    - `grep "useSpeciesOrder" mobile/src/screens/TreeRegistrationScreen.tsx` returns a match
    - `grep "usePhotoCapture" mobile/src/screens/TreeRegistrationScreen.tsx` returns a match
    - `grep 'testID="tree-count"' mobile/src/components/TreeRegistrationHeader.tsx` returns a match
    - `grep 'testID="undo-button"' mobile/src/components/LastThreeTrees.tsx` returns a match
    - `grep 'testID="finalize-button"' mobile/src/screens/TreeRegistrationScreen.tsx` returns a match
  </acceptance_criteria>
  <done>TreeRegistrationScreen.tsx is under 300 lines, imports only from hooks/ (not repositories/queries/services), uses useTreeRegistration + useNNFlow + useSpeciesOrder + usePhotoCapture, and has all three testIDs for Maestro E2E flows.</done>
</task>

</tasks>

<verification>
- `wc -l mobile/src/screens/TreeRegistrationScreen.tsx` less than 300
- `grep -c "import.*from '.*repositories/\|import.*from '.*queries/\|import.*from '.*services/" mobile/src/screens/TreeRegistrationScreen.tsx` equals 0
- All three testIDs present across screen and component files
- `cd mobile && npx jest --ci --no-coverage` passes (no regressions)
</verification>

<success_criteria>
TreeRegistrationScreen.tsx is a thin composition layer under 300 lines with zero CLAUDE.md rule 9 violations and all Maestro testIDs preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-strategy/09-07-SUMMARY.md`
</output>
