---
phase: 08-login-offline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mobile/src/services/OfflineAuthService.ts
  - mobile/tests/setup.ts
  - mobile/tests/auth/offlineAuth.test.ts
autonomous: true
requirements:
  - OFFL-01
  - OFFL-02
  - OFFL-03
  - OFFL-04
  - OFFL-05
  - OFFL-06
  - OFFL-07

must_haves:
  truths:
    - "cacheCredential stores a salted SHA-256 hash, never plaintext"
    - "verifyCredential returns role on correct password, null on wrong"
    - "clearCredential removes only the specified email entry"
    - "getCachedEmails returns email list without passwords or hashes"
    - "Old saved_accounts key is deleted on first getCachedEmails call"
  artifacts:
    - path: "mobile/src/services/OfflineAuthService.ts"
      provides: "Credential cache service with hash/verify/clear/list"
      exports: ["cacheCredential", "verifyCredential", "clearCredential", "getCachedEmails"]
    - path: "mobile/tests/auth/offlineAuth.test.ts"
      provides: "Unit tests covering OFFL-01 through OFFL-07"
      min_lines: 80
    - path: "mobile/tests/setup.ts"
      provides: "Extended expo-crypto mock with digestStringAsync and getRandomBytes"
      contains: "digestStringAsync"
  key_links:
    - from: "mobile/src/services/OfflineAuthService.ts"
      to: "expo-crypto"
      via: "digestStringAsync and getRandomBytes"
      pattern: "Crypto\\.digestStringAsync"
    - from: "mobile/src/services/OfflineAuthService.ts"
      to: "expo-secure-store"
      via: "getItemAsync/setItemAsync/deleteItemAsync"
      pattern: "SecureStore\\.(get|set|delete)ItemAsync"
---

<objective>
Create OfflineAuthService — a pure service that manages salted SHA-256 credential hashes in SecureStore — and comprehensive unit tests covering all OFFL requirements.

Purpose: Establish the credential cache layer that useAuth and login.tsx will consume in Plan 02. TDD ensures correctness before wiring.
Output: OfflineAuthService.ts with 4 exported functions, offlineAuth.test.ts with 7+ test cases, extended expo-crypto mock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-login-offline/08-RESEARCH.md

@mobile/src/supabase/auth.ts
@mobile/tests/setup.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OfflineAuthService with TDD</name>
  <files>mobile/tests/setup.ts, mobile/src/services/OfflineAuthService.ts, mobile/tests/auth/offlineAuth.test.ts</files>
  <read_first>
    - mobile/tests/setup.ts (current expo-crypto mock — must extend)
    - mobile/src/supabase/auth.ts (SecureStore key patterns, clearSession)
    - .planning/phases/08-login-offline/08-RESEARCH.md (Pattern 1: OfflineAuthService)
  </read_first>
  <behavior>
    - cacheCredential("user@test.com", "pass123", "tecnico") stores entry in SecureStore under key "offline_credentials" with {email, hash, salt, role} — hash is NOT "pass123"
    - cacheCredential called twice with same email updates (not duplicates) the entry
    - verifyCredential("user@test.com", "pass123") returns "tecnico" after cacheCredential
    - verifyCredential("user@test.com", "wrongpass") returns null
    - verifyCredential("unknown@test.com", "pass123") returns null (no cached entry)
    - clearCredential("user@test.com") removes only that email; other entries remain
    - getCachedEmails() returns ["user@test.com"] after cacheCredential; returns [] when empty
    - getCachedEmails() deletes the old "saved_accounts" SecureStore key on call (lazy migration)
    - Multiple users: cache user1 and user2, clear user1, verify user2 still works
  </behavior>
  <action>
    **Step 1: Extend expo-crypto mock in tests/setup.ts.**
    Add to the existing `jest.mock('expo-crypto', ...)` block:
    ```
    CryptoDigestAlgorithm: { SHA256: 'SHA-256' },
    digestStringAsync: jest.fn(async (_alg, data) => `mock-hash-${data}`),
    getRandomBytes: jest.fn((n) => new Uint8Array(n).fill(42)),
    ```
    Keep the existing `randomUUID` mock.

    **Step 2: Write tests FIRST in mobile/tests/auth/offlineAuth.test.ts.**
    Import from `../../src/services/OfflineAuthService`. Mock SecureStore with an in-memory store (use the existing jest.mock from setup.ts but configure mockImplementation in beforeEach to simulate get/set/delete on a local Map).

    Test cases (map to OFFL IDs):
    - "caches credential with hash, not plaintext" (OFFL-02, OFFL-07): call cacheCredential, read raw SecureStore value, assert no plaintext password, assert hash field exists
    - "verifies correct password" (OFFL-03): cache then verify, expect role returned
    - "rejects wrong password" (OFFL-04): cache then verify with wrong pw, expect null
    - "rejects unknown email" (OFFL-04): verify uncached email, expect null
    - "clears only target user, others remain" (OFFL-05): cache 2 users, clear one, verify other still works
    - "getCachedEmails returns email list" (OFFL-02): cache 2 users, expect array of emails
    - "getCachedEmails deletes old saved_accounts key" (OFFL-07): pre-set saved_accounts in store, call getCachedEmails, assert deleteItemAsync called with "saved_accounts"
    - "updates existing entry on re-cache" (OFFL-02): cache same email twice, getCachedEmails returns 1 entry

    Run tests — they MUST FAIL (RED phase — OfflineAuthService doesn't exist yet).

    **Step 3: Implement OfflineAuthService in mobile/src/services/OfflineAuthService.ts.**
    Follow the research Pattern 1 exactly:
    - `const OFFLINE_CREDENTIALS_KEY = 'offline_credentials';`
    - `const SAVED_ACCOUNTS_KEY = 'saved_accounts';` (for lazy deletion)
    - Type `OfflineCredential = { email: string; hash: string; salt: string; role: string; }`
    - Private `getAll()`: reads and parses OFFLINE_CREDENTIALS_KEY from SecureStore
    - `cacheCredential(email, password, role)`: generates 32-byte salt via `Crypto.getRandomBytes(32)`, converts to hex string, hashes `password + salt` with `Crypto.digestStringAsync(SHA256, ...)`, upserts entry by email
    - `verifyCredential(email, password)`: finds entry by email, hashes `password + entry.salt`, compares hash, returns role or null
    - `clearCredential(email)`: filters out email from array, writes back
    - `getCachedEmails()`: returns `all.map(c => c.email)`, also calls `SecureStore.deleteItemAsync(SAVED_ACCOUNTS_KEY)` for lazy migration

    Run tests — they MUST PASS (GREEN phase).

    **Step 4: Verify no function exceeds 20 lines** (CLAUDE.md rule). Refactor if needed.
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Trabajos/BaykaApp/bayka-app-redesign/mobile && npx jest tests/auth/offlineAuth.test.ts --testEnvironment node --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "cacheCredential" mobile/src/services/OfflineAuthService.ts
    - grep -q "verifyCredential" mobile/src/services/OfflineAuthService.ts
    - grep -q "clearCredential" mobile/src/services/OfflineAuthService.ts
    - grep -q "getCachedEmails" mobile/src/services/OfflineAuthService.ts
    - grep -q "digestStringAsync" mobile/tests/setup.ts
    - grep -q "getRandomBytes" mobile/tests/setup.ts
    - grep -q "saved_accounts" mobile/src/services/OfflineAuthService.ts (lazy delete reference)
    - grep -qv "password.*=.*password" mobile/src/services/OfflineAuthService.ts (no plaintext storage)
    - All 8+ test cases pass
  </acceptance_criteria>
  <done>
    OfflineAuthService exports cacheCredential, verifyCredential, clearCredential, getCachedEmails.
    All unit tests pass. No plaintext password is stored. Old saved_accounts key is lazily deleted.
    expo-crypto mock in setup.ts includes digestStringAsync and getRandomBytes.
  </done>
</task>

</tasks>

<verification>
cd /Users/facu/Desarrollos/Trabajos/BaykaApp/bayka-app-redesign/mobile && npx jest tests/auth/ --testEnvironment node --no-coverage
</verification>

<success_criteria>
- OfflineAuthService.ts exists with 4 exported functions
- All offlineAuth.test.ts test cases pass
- expo-crypto mock extended in tests/setup.ts
- No plaintext passwords stored anywhere in OfflineAuthService
- Lazy deletion of old saved_accounts key implemented
</success_criteria>

<output>
After completion, create `.planning/phases/08-login-offline/08-01-SUMMARY.md`
</output>
