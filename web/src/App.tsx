import styles from './App.module.css';

/**
 * Placeholder del scaffold: las rutas reales (login, plantaciones, dashboard)
 * llegan en los issues siguientes del milestone "Web de gestión (v1)".
 */
export function App() {
  return (
    <main className={styles.placeholder}>
      <h1>Bayka Gestión</h1>
      <p>Sitio web de gestión de plantaciones — en construcción.</p>
    </main>
  );
}
