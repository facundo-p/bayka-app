-- Pendientes varados (#638). Solo local: por qué lo pendiente de la plantación no
-- puede subir (finalizada, archivada, sin permiso). Null = nada varado.
ALTER TABLE `plantations` ADD `motivo_varado` text;
