// Estilos de OrangeDot.
import { StyleSheet } from 'react-native';
import { colors } from '../theme';

export const orangeDotStyles = StyleSheet.create({
  dot: {
    backgroundColor: colors.syncPending,
  },
});
