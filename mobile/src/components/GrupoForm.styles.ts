import { StyleSheet } from 'react-native';
import { spacing } from '../theme';

export const grupoFormStyles = StyleSheet.create({
  actions: {
    flexDirection: 'row',
    gap: spacing.xl,
    marginTop: spacing.md,
  },
});
