Pantalla general

- [ ]  card de plantación debería mostrar cuantos árboles lleva cargados hoy el usuario

Pantalla de plantación

- [ ]  Debería tener una opción de "resolver todos los N/N", que vaya mostrando todos los N/N de todos los subgrupos, permitiendo identificar a c/u.
- [ ]  Cards con los que se listan subgrupos de la plantación son exageradamente grandes. Reducir su altura a la mitad.
- [x]  Cada card debería indicar la cantidad de árboles N/N que tiene cargados.
- [ ]  La identificación de un N/N debería actualizar ese contador automáticamente.
- [ ]  Cada card de subgrupo debería mostrarse un indicador de cantidad de árboles del subgrupo.
- [ ]  Debería mostrarse, debajo del Nombre(lugar) de la plantación, el total de árboles cargados en la plantación, y el total cargados por el usuario el día de hoy.
- [ ]  Debería permitirse borrar un Subgrupo, pidiendo confirmación (en caso de tener árboles registrados, avisar)
- [x]  Remover botón de "finalizar" de la card del sub grupo.
- [x]  Mostrar en la card del sub grupo un indicador de qe hay árboles n/n por resolver
- [x]  Subgrupo se escribe sin la G mayúscula
- [ ]  El botón de "nuevo subgrupo" se superpone con el listado de cards de subgrupos. No debería pasar.

Pantalla de Sub Grupo

- [x]  Remover indicadores de cantidad de n/n (mostrar la info en la primer row de la pantalla)
- [ ]  Remover opción de resolver n/n (se mostrará en la pantalla de la plantación, permitiendo resolver los n/n de todos los subgrupos)
- [ ]  Grilla de 3 botones por fila (no de 4)
- [ ]  Hay 2 rows al inicio de la pantalla (uno dice "subgroup" y el otro "Registro de Árboles") que hacen lo mismo, volver a la pantalla anterior. Dejar 1 solo, que tenga la flechita
para volver, el nombre de la linea (centrado) y la cantidad de n/n. De esta manera se simplifican las 3 rows iniciales en una sola
- [ ]  El listado de las últimas 3 especies cargadas tiene que ser más pequeño. Destinarle menos espacio. Considerar mostrarlos no como listado vertical, sino mostrarlos horizontalmente,
parecido a como se ven los botones de la grilla (pero agregando el número de árbol)
- [ ]  No permitir finalizar un subgrupo si no tiene árboles cargados.
- [ ]  Permitir eliminar el subgrupo. Preguntar por confirmación activa (un slider o algo así, que requiera una acción más evidente que apretar el "Ok") para diferenciarlo del botón de
"Finalizar" y no confundirlos por error.
- [ ]  Todos los botones deberían tener el mismo tamaño.
- [ ]  Deberían vibrar al presionarse
- [ ]  Debería actualizarse automáticamente la lista de los últimos 3 árboles plantados (no se está actualizando)
- [ ]  No entiendo qué es el row que dice [id] con flecha para volver atrás
- [ ]  Debería poder accederse a ver todo el listado de árboles cargados
- [ ]  Debería permitirse borrar árboles cargados. Al hacerlo, se actualizan las posiciones de todos los árboles para que queden consecutivas

Pantalla de resolución de n/n

- [ ]  La primer row con "N/N 1 de x" y con la info del subgrupo del árbol, debe ser una fix row (que no se oculte al scrollear)
- [ ]  El botón de "guardar" tiene que estar fijo también (porder verlo y apretarlo sin scrollear)
- [ ]  Mismo problema que la pantalla de Subgrupo: las 2 rows iniciales hacen lo mismo. Dejar una sola con el texto "Volver"

Nav bar

- [ ]  En la barra de navegación, no se ven los íconos de las opciones "Plantaciones" y "Perfil" (en los perfiles de admin no se ven tampoco ningua de las 3 opciones con íconos)